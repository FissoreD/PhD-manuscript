\chapter{Context: languages and tools}
\label{chap:context}

At the core of this manuscript, there are two important pieces of software. 
On the one hand, we
consider the \rocq proof assistant (formerly known as \coq); on the other hand,
we consider the logic programming language \elpi.

\rocq~\cite{coqart} is one of the main proof assistants. It is based on
type theory and allows formal reasoning about mathematical statements
and program specifications. In this setting, logical propositions are
interpreted as types, and proofs are interpreted as programs (or terms) of
these types. The underlying type theory of \rocq~\cite{cc-Coquand} ensures that the coherence
between a program and its type is checked, thus providing
guarantees of correctness.
Proof development in \rocq is carried out using a language of tactics, which
allows the user to construct proofs interactively.

On the other hand, \elpi~\cite{dunchev15} is a language from the field of logic
programming. In particular, it is a dialect of \lprolog~\cite{miller1988} which features
backtracking, higher-order programming and unification, backtracking control
though the \cut operator, unification control throuh predicates modes. 
A plugin, called \rocqelpi~\cite{tassi2018}, allows one to extend \rocq
with new commands and tactics~\cite{krebbers2025,maas2024,tassi2023}.

\emph{Commands} extend the \rocq language by introducing new definitions or
theorems that are automatically generated by \elpi programs. Examples of
libraries that intensively use this feature include \derive~\cite{tassi2019},
which derives equality theorems for inductive types, and \hb~\cite{hb}, which helps define
complex algebraic structures while automatically generating associated theory.
\hb supports the \mc~\cite{assia2022} ecosystem and
mechanizations such as the Odd Order and Four Color theorems
\cite{gonthier2013,gonthier2023}.

\emph{Tactics} extend the proof system of \rocq{} by allowing additional
forms of automation during proof development. They are implemented on top of
the \elpi{} interpreter.
The \trocq~\cite{enzo2023,enzo2024} library provides an \elpi-based tactic for
automatic reasoning using proof transfer techniques. The \algtac~\cite{algtac}
library instead offers \elpi-based tactics to reason about computational
reflection for algebraic structures such as rings and lattices.

In the following sections, we introduce the key concepts that form the
foundation of \cref{chap:tc,chap:ho-for-free,chap:det-elpi,chap:mechanization}.
In particular, in \cref{sec:rocq-intro}, we briefly discuss about elaboration
and in particular about type-classes, a central component
of a proof assistant such as \rocq. In
\cref{sec:the-elpi-language}, we present the core aspects of the \elpi language,
including its syntax and semantics, which are necessary to understand its
execution model. Finally, in \cref{sec:coq-elpi}, we describe the main ideas
behind the \rocqelpi plugin.

\section{The bases of the \rocq proof assistant}
% TODO: vedere dove è usata
\label{sec:rocq-intro-lam-calc}

As anticipated, the \rocq language is based of type theory~\cite{coq-huet}. At
the heart of this research area, we have the pure $\lambda$-calculus, thoerized
by Church~\cite{church} in 1932.
It is a small language made of \emph{terms}, their abstract syntax is given below.
%
$$t := x\ |\ t\ t\ |\ \lambda x.t$$

The smallest term is a variable $x$; term applications is noted $t\ t$, we call
the first term the head of the application and the second the argument; a
function, abstracts a variable $x$ in a term $t$, and it is noted $\lambda x.
t$. We say that the term is binding $x$ in $t$, equivalently, we say that the
variable $x$ is \emph{bound} in $t$, a variable that does not occur under a
$\lambda$-abstraction is said to be \emph{free}. A \emph{subterm} is a term
appearing inside a term $t$.

For example the term $\lambda x. \lambda y. x$, also called the \texttt{fst}
function, has two bound variables $x$ and $y$. The term $\lambda y. x$ is a
subterm of \texttt{fst} and it has a free variable $x$.

The evaluation of a $\lambda$-term is performed by a set of \emph{reduction}
rules. If two terms reduce to the same term then, they are equivalent.
The interesting reductions rules for this manuscript are:
%
\begin{center}
  \begin{bprooftree}
    \AxiomC{$~$}
    % \AxiomC{$t_1\ [x\backslash t_2] \triangleright t_3$}
    \RightLabel{\rulelabel{$\beta$-rule}}
    \UnaryInfC{$(\lambda x. t_1)\ t_2 =_\beta t_1[x\backslash t_2]$}
  \end{bprooftree}
  \begin{bprooftree}
    % \AxiomC{$\lambda x. t\ x \triangleright t \quad x \text{ is not free in t}$}
    \AxiomC{$x \text{ is not free in t}$}
    \RightLabel{\rulelabel{$\eta$-rule}}
    \UnaryInfC{$\lambda x. t\ x =_\eta t$}
  \end{bprooftree}
\end{center}

The $\beta$-rule deals with terms application when the head contains a
$\lambda$-abstraction. This application evaluates to the head where all
free occurrences of the abstraction $x$ are replaced by the argument of the
application. We say
that a term on which the $\beta$-rule can be used is a $\beta$-redex. A term
that can not be $\beta$-reduced, is in $\beta$-normal form. For example, the
application $((\lambda x. \lambda y. x)\ 1)\ 2$ is a $\beta$-redex: its
reduction produces the term $(\lambda y. 1)\ 2$. This term is stil a
$\beta$-redex and can be further reduced to $1$. 

The $\eta$-rule deals with the abstraction of a variable $x$ over term
application $t\ x$ where $x$ is not used free $t$. Similarly to the
$\beta$-rules, a term that can be $\eta$-reduce is a $\eta$-redex otherwise
it is in $\eta$-normal form.

In the domain of type-thoery, pure $\lambda$-calculus is extended with a system
of typing rules, imposing constraint about the \emph{type} that the argument of
a function should have. This extension of the $\lambda$-calculus is called
simply-typed $\lambda$-calculus~\cite{church-type}.
%
$$\tau := \alpha\ |\ \tau \to \tau$$

The syntax of types is given above. A type belongs either to a finite set of
base types, noted $\alpha$, or to the function space, noted $\tau \to \tau$,
where the first $\tau$ represents the domain of the function and the second
$\tau$ represents the codomain.
In this calculus, we annotate $\lambda$-abstractions with their type,
therefore, terms become: 
%
$$t := x\ |\ t\ t\ |\ \lambda (x : \tau).t$$

A \emph{typing environment}, usually noted $\Gamma$, is a partial mapping
from variables to their type. We use the notation $\Gamma + (x:\tau)$ for the
insertion of a new variable $x$ with type $\tau$ in the environment $\Gamma$.
The typing rules are the following:

\begin{center}
  \begin{bprooftree}
    \AxiomC{$x : \tau \in \Gamma$}
    \RightLabel{\rulelabel{$T_x$}}
    \UnaryInfC{$\Gamma \vdash x : \tau$}
  \end{bprooftree}
  \begin{bprooftree}
    \AxiomC{$\Gamma \vdash t_1 : \tau_1 \to \tau_2 \quad \Gamma \vdash t_2 : \tau_1$}
    \RightLabel{\rulelabel{$T_@$}}
    \UnaryInfC{$\Gamma \vdash t_1\ t_2 : \tau_2$}
  \end{bprooftree}
  \begin{bprooftree}
    \AxiomC{$\Gamma + (x : \tau) \vdash t : \tau'$}
    \RightLabel{\rulelabel{$T_\lambda$}}
    \UnaryInfC{$\Gamma \vdash \lambda (x: \tau). t : \tau \to \tau'$}
  \end{bprooftree}
\end{center}

These typing rules are used to determine the type of a term $t$, in particular,
the notation $\Gamma \vdash t : \tau$ stands for a term $t$, with type $\tau$ in
a typing environment $\Gamma$. If it is possible to derive a type from a term,
we say that $t$ \emph{typechecks}. The \emph{typechecker} is the name of the
algorithm aiming to typecheck a term.

The simply-typed lambda caluclus can be extended to encode 
types depending on types, types depending on temrs
and terms depending on tyeps. The set of these extensions is graphically presented
by the Barendregt cube~\cite{cube}. An expansion which is worth explaining in this
paragraph is the polymorphic $\lambda$-calculus. In this calculus, terms
can depend on types, and allow to encode the so-called \emph{polymorphic
$\lambda$-calculus}~\cite{systemf} (also called \emph{System F}).

The symplest example, is the identity function, which, in pure $\lambda$-calculus
is encoded $\lambda x.x$, it is a function returning its input. The type of the
codomain depends on the type of its input. In the simply-typed
$\lambda$-calculus, we should give a unique type to the abstraction, for example
\cI{boolean}, but this function could also work on \cI{nat} numbers.

The polymorphic $\lambda$-calculus gives the possibility of
quantifying types in functions, and having types depending on types.
We extend the term and typing syntax to account for this new change:
%
$$t := \dots\ |\ \tau$$
$$\tau := \dots\ |\ \forall (x: \tau). \tau$$

The new $\forall$-constructor allows for the quantification of types in another
type $\tau$. This is the typing equivalent to the $\lambda$-abstractions over
terms. In order to quantify types in terms, we use the shorcut, $\lambda (x:
T).t$ to represent the quantification of a type called $T$ in the term $t$.
Now the identity function can be encoded $\lambda (x:T).\lambda (y:x).y$.

In order to take into account the introduction of polymorphic
$\lambda$-calculus, we extends the typing system with the following typing rules:
%
\begin{center}
  \begin{bprooftree}
    \AxiomC{$T \text{ not in } \Gamma \quad \Gamma + (x : T) \vdash t : \tau$}
    \RightLabel{\rulelabel{$T_\forall$}}
    \UnaryInfC{$\Gamma \vdash \lambda (x:T).t : \forall (x: T). \tau$}
  \end{bprooftree}
\end{center}

Now we can use the polymorphic definition of the identify function as follows:
$(\lambda (x : T). \lambda (y : x). y)\ \texttt{nat}\ 3$. The $\beta$-reduction
steps on this term produce: $(\lambda (y : \texttt{nat}). y)\ 3$ and then $3$.

In this previous example, we have explicitly passed the type \texttt{nat} as the
first argument of the identity function, however, in a typing environment where
$3$ is mapped to the type \texttt{nat}, we can deduce that the first argument of
the identify function should be \texttt{nat}, simply by looking to the type of
$3$. Therefore, a user of the language would like to write: $(\lambda (x :
T). \lambda (y : x). y)\ \_\ 3$, leaving an underscore as missing information in
term that is to be \emph{inferred} by the typechecker. This missing information
is sometimes called a \emph{hole} and represents an existential variable (or
evar) that should be instantiated by the typechecker.
In a system such as \rocq, the underscore can be avoided if the
argument of the function is marked as implicit.

This is a simple example, but the system can be instrumented with several rules
allowing the user to take a lot of advantage from the possibility of leavining
missing information in terms.

\section{Elaboration in the \rocq proof assistant}
% TODO: vedere dove è usata
\label{sec:rocq-intro}

\begin{figure}
\includegraphics[width=1\textwidth]{elaboration.pdf}
\caption{Elaboration in \rocq}
\label{fig:elaboration}
\end{figure}

In this section, we focus on a more technical aspect that enables communication
between the user and the \rocq system: the \emph{elaborator}~\cite{elaboration}. 
This component is
part of all proof assistants. Its purpose is to give the user a certain amout of
freedom about the possibility of entrering incomplete or ambiguous expressions,
leaving the system to infer the missing information and resolve ambiguity.

Figure~\ref{fig:elaboration}, taken from~\cite{elaboration}, summarizes the main
steps of the elaboration process. Starting from a raw input string, the system
parses it into a \rocq term that may still be incomplete, and then elaborates it
into a fully specified term. This final term is passed to the kernel, which
typechecks it. If the check succeeds, the term is a valid inhabitant of its
type, or in other words a proof of the given statement. This process includes
several key tasks: notation resolution, type inference, coercion insertion, and
inference of overloaded symbols.

\emph{Notation} resolution removes syntactic ambiguities by replacing
user-defined notations with their corresponding logical symbols in the abstract
syntax tree. The resulting term is unambiguous but may still contain holes,
which are then filled during later stages of the elaborator. For example the
term \cI{[1, 0]}, which uses standard mathematical syntax to represent an
ordered collection of elements, contains notations to represent natural numbers
and lists. The elaborated term becomes:
%
\begin{center}
  \cI{@cons _ (S O) (@cons _ O (@nil _))}
\end{center}

\noindent
where \cI{cons} and \cI{nil} are term constructor representing respectively the
function inserting in head position of a list an eleemnt, and the empty list.
And \cI{S} and \cI{O} represent respectively a function returning the successor
of its argument and the costant for the $0$ natural number.

\emph{Type inference}, as announced in the previous section,
is a mechanisms made of a system of rules allowing to
deduce missing information of a term from a typing environment.

As an exapmle, we provide in the following table an example of
typing environment, where the type of the constant used in the notation above
are explicitely given.
%
\begin{center}  
\begin{tabular}{ccc}
  Symbol & & Type \\
  \hline
  \cI{cons} & : & \cI{~$\forall$~ T, T ~$\to$~ list T ~$\to$~ list T} \\
  \cI{nil}  & : & \cI{~$\forall$~ T, list T} \\
  \cI{S}    & : & \cI{nat ~$\to$~ nat} \\
  \cI{O}    & : & \cI{nat}
\end{tabular}
\end{center}

The execution of the typechecker on the term above returns,
the elaborated term:
%
\begin{center}
\cI{@cons nat (S O) (@cons nat O (@nil nat))}
\end{center}

\textit{Coercions} are a mechanism~\cite{barthe95}
to implicitly convert a term of type $t_1$ into a term of
type $t_2$ by inserting a function of type $t_1 \to t_2$.
For instance, suppose that \cI{nat_to_bool} maps \cI{0} to \cI{false} and any
other natural number to \cI{true}, and is declared as a coercion from
\cI{nat} to \cI{bool}. Then the term \cI{negb 0}, where \cI{negb} expects an
argument of type \cI{bool}, is automatically elaborated into
\cI{negb (nat_to_bool O)}.

Beyond type inference and coercions, a key feature of elaboration is support for
overloading. This allows the same symbol to be reused at different types. Since
ad-hoc polymorphism plays an important role in this manuscript, we will examine
this mechanism in more detail in the following subsection.


\subsection{Ad-hoc polymorphism}
\label{sec:ad-hoc-poly}

Polymorphism is a fundamental feature of functional programming languages. In
his lecture notes, Strachey~\cite{polymorphism} distinguishes between two main
forms of polymorphism: parametric polymorphism and ad-hoc polymorphism.
Parametric polymorphic functions are \emph{uniform}, meaning that their behavior
does not depend on the specific type at which they are instantiated. For
example, the function \texttt{length}, with type \cI{list A ~$\to$~ int}, 
returns $2$
when applied to both lists \cI{[1, 0]} and \cI{["foo", "bar"]}.
In other words, the compiler
reuses the same implementation of \texttt{length} for every call, regardless of
the instantiation of \cI{A}.

Parametric polymorphism is widely used in functional programming, but it is
not well suited to model overloading as it appears in mathematics. In
mathematical notation, a symbol such as $+$ has a consistent and intuitive
meaning, and no ambiguity arises for the reader. For example, in $1 + 2$ the
symbol denotes addition over integers, while in $\pi + e$ it denotes addition
over real numbers. The same symbol is used for operations that share common
algebraic properties.

In contrast, parametric polymorphism does not allow to
have multiple different type-dependent interpretations. This limitation appears in
languages such as \ocaml, where integer and floating-point additions
are represented by distinct operators, $+$ and $+.$, respectively. Thus, one
writes $1 + 2$ for integers and $\pig +\!.\ e$
% \footnote{In \ocaml, $\pig$ is \mintinline{ocaml}{Float.pi} and $e$ is \mintinline{ocaml}{Float.exp 1.0}}
for floating-point numbers.
In
general, a different symbol must be introduced for each type implementing a
given mathematical operation.

To answer the need of a common notation for operations and properties across
different types, Wadler and Blott introduced type classes in 1989
\cite{wadler1989-tc}. This idea was later implemented in Haskell
\cite{wadler1996-hs}, where it quickly became an important and widely used
feature of the language.

After that, several other languages adopted similar mechanisms. For example,
\cpp~\cite{cpp-tc}, \java~\cite{java-tc}, and even proof assistant such as
\rocq~\cite{sozeau2008-tc}, and \lean~\cite{lean-tc}, extended their type
systems to support this form of overloading.

% \subsection{Ad-hoc polymorphism: Type classes}

\paragraphTwo{Symbol overloading}
Type classes provide a principled form of ad-hoc polymorphism, enabling
a single symbol to be associated with multiple type-specific implementations
while preserving static type safety.
They provide structure parameterized by one or more types~$t$ and
declare the signatures of a set of functions, called methods, whose
types may depend on~$t$. An instance of a type class provides a concrete
implementation of the type class for a specific choice of~$t$, providing
definitions for all of its methods.

Unlike parametric polymorphism, where a unique function is used for the
execution of a call, type classes rely on instance resolution. Typeclasses and
instances are stored in a database mapping classes to their instances. When solving
a call to a type class method, the elaborator finds the right implementation by
selecting the instance of the type class associated with the given type in the
given database.

As an example, consider the type class \cI{Add}, parameterized by a
type \cI{T} and equipped with a method \cI{plus} to perform addition between
two elements of type \cI{T}. In \rocq, the definition of \cI{Add} is given
below.

\mcenter{code/plus_tc_add.tex}

The type of the method \cI{plus} is
$\forall \cI{T},\ \cI{Add T} \to \cI{T} \to \cI{T} \to \cI{T}$,
where both the type parameter \cI{T} and the instance argument
\cI{Add T} are implicit. The typechecking of the term
\cI{plus 3 4} gives:
%
\begin{center}
  \cI{@plus nat ?Add 3 4 : nat where ?Add : [ |- Add nat]}
\end{center}

The term has type \cI{nat}, but the instance for \cI{Add nat} cannot be
inferred (the \cI{?Add} in the snippet above). This is beacuse in the type-class
database, there is no instance associated to \cI{Add}. To resolve this,
given the function \cI{Nat.add} of type \cI{nat ~$\to$~ nat ~$\to$~ nat} computing
the sum between natural numbers, we
define the instance:

\mleft{code/plus_tc_anat.tex}

The declaration \cI{addNat} has type \cI{Add nat} and provides an
implementation of the method \cI{plus}. Registering this instance extends
the instance database for \cI{Add} with an entry for the type \cI{nat}.
Afterwards, the term \cI{plus 3 4} can be elaborated successfully: 
%
\begin{center}  
  \cI{@plus nat addNat 3 4 : nat}
\end{center}

\noindent
Note how \cI{?Add} is now inferred to the instance \cI{addNat}.

Similarly, given the function \cI{Rplus} of type \cI{R ~$\to$~ R ~$\to$~ R}
computing the sum between real numbers, we can define an instance for
reals:

\mleft{code/plus_tc_ar.tex}

The same symbol \cI{plus} can then be applied to values of type \cI{nat} or
\cI{R}. Therefore, extending the notation system, we are now allowed to write $1
  + 2$ and $\pig + e$ knowing that the elaborator automatically selects the
appropriate instance, \cI{addNat} or \cI{addR}.

A key feature of type classes is the ability to define \emph{conditional}
instances, whose availability depends on other instances. An example
is the instance for the product type.

\input{code/plus_tc_add_prod.tex}

The type of the instance \cI{addProd} states that it implements \cI{Add (T1 *
  T2)} provided that instances of \cI{Add T1} and \cI{Add T2} are available. In
this way, addition on pairs is derived from addition on their components, that
is, we can add $(\pig, 3)$ with $(e, 4)$, $(3,(\pig, 4))$ with $(7, (e, 0))$~\dots

% \paragraphTwo{Theory overloading}

The type class \cI{Add} can be defined in a similar style in
\haskell, up to minor syntactic differences. In a proof assistant such as
\rocq, however, the same intuition behind symbol overloading
can be reused not only to overload function by also to 
overload but also theorems.

For instance, one may enrich \cI{Add} with the associativity specification:
%
\input{code/plus_tc_add1.tex}

An instance of this refined class must now provide both an implementation of the
function \cI{plus} and a proof that \cI{plus} is associative. Then, if
\cI{addNat}, \cI{addR} and \cI{addProd} are implemented according the new
definition of \cI{Add}, the \cI{assoc} symbol can be used as a proof that
addition is associative over natural and real numbers and pairs.

% In order to better structure type classes, the system can be extended so that
% type classes can inherit properties of existing classes. In group theory this
% feature consist, for example, in defining an object called \cI{Magma} with an
% operation \cI{op}, and a second opject \cI{Semigroup} which extends \cI{Magma}s
% by postulating the associative property on \cI{op}.

% This way for example, the \cI{Add} class, in some developments is formalized as:

% \input{code/magma.tex}

\paragraphTwo{Search strategy}

Type classes are used in several major \rocq libraries, such as \tlc~\cite{tlc},
\stdpp~\cite{stdpp}, and \iris~\cite{iris-junk}. They provide access to the type-class
solver, which automatically infers instances by searching in a database of
declared instances. This database is organized as an ordered list. 

The priority of an instance can be specified at the end of its declaration
using \cI{| N}, where \cI{N} is an integer. The instance is then placed at
position \cI{N} in the database. A smaller value of \cI{N} means higher
priority. If no priority is given, \rocq computes it automatically by
assigning weight $1$ to each premise that is itself a type-class constraint.

Given a type-class goal, the solver uses the instance in the database as a base
of knowledge. It then exploit a DFS-like search strategy close to the \prolog
SLD~\cite{sld} algorithm, where the conclusion of each instances is tried
for unification with the current goal. In case of success, the hypotheses
of the selected instance
are tried. If every hypothesis of the instance succeeds, the selected instance
is a solution for the given goal. Otherwise, in case of a failure, the
search algorithm backtracks to the first non-explored goal in the search space.
Until either, finding a solution or exausting every possibile alternative.
As a consequence, the order of instances in the database is important, since it
may affect both performance and termination.

As an example, consider the instances defined previously for the type class
\cI{Add}. We suppose that they ordered in as follows: \cI{addNat, addR,
addProd}. To solve the goal \cI{Add (nat * R)}, the solver first tries
\cI{addNat}, which fails because its conclusion does not unify with the goal. The
same happens with \cI{addR}. The instance \cI{addProd} can be applied if its
premises, namely \cI{Add nat} and \cI{Add R}, can be solved. Since both are
derivable, the resulting instance is \eI{addProd nat R addNat addR}. 

If the users (wrongly) gives the highest priority to the instance \cI{addProd},
ang given a goal \cI{Add ?X}, where \cI{?X} is an existential variable. The
resolution of such a goal in the type-class solver leads to non-termination. In
such a case, the instance \cI{addProd} is used, assigning \cI{?X} to the pair
\cI{?X1 * ?X2}. Then, the first premise of \cI{addProd} raise the recursive
search for \cI{Add ?X1}. After using, again, the instance \cI{addProd}
\cI{X1} is instanceated to \cI{?X3 * ?X4} and so on. This procedure loops
indefinetly.

Several mechanisms are available to guide the solver. One common tool is
\emph{modes}, which enforce conditions on the shape of class arguments during
search. There are three kinds of modes in \rocq. Informally, a mode is a list
of symbols \texttt{+}, \texttt{!}, or \texttt{-}, indicating whether an
argument is treated as an input, partially known (its head is fixed), or as
an output.

A type class may have several modes. During instance search, these modes are
checked dynamically: the search proceeds if at least one mode is satisfied by
the current goal.

A characteristic of type classes is that several instances may match the same
goal. This requires backtracking in the solver. In some libraries, such as
\stdpp, this behavior is exploited to mimic logic programming. For example,
the type class \cI{SetUnfold} is used to transform set expressions into
logical properties. For instance, an inclusion $X \subseteq Y$ can be turned
into a statement of the form $\forall x, P\ x \to Q\ x$, under suitable
assumptions on $X$ and $Y$. If no transformation applies, the default instance
\cI{set_unfold_default P : SetUnfold P P} is used, leaving the goal unchanged.

This default instance serves as a catch-all rule, as it applies to any goal.
Although backtracking can be useful in such patterns, it may introduce
ambiguities and lead to unexpected behavior that is difficult to control in
practice.

In \rocq, backtracking search of a class can be disabled using the flag
\texttt{Typeclasses Unique Instances}. When a solution to the typeclass goal of
this typeclass is found, we never backtrack on it, the runtime assumes that it is
canonical.

% However,
% this mechanism is not class-specific: once enabled, non-backtracking is applied
% to all type classes involved in the search. In practice, a user may prefer a
% more precise behavior, where only certain type classes avoid backtracking, while
% others retain it. Therefore, at least in the libraries we have analized, whis
% flag is not used.

% Moreover, this flag is not sufficient to prevent the user from control
% unsepected behavior that can come from the import of different files. Imagine


% \paragraphTwo{Canonical structures} In \rocq, it exists another, deterministic
% way, allowing ad-hoc polymorphism called Canonical Structures~\cite{cs},
% introduced by a Saïbi~\cite{cs0}. Canonical structures extends unification
% unification algorithm of the \rocq elaborator. While typechecking two symbols,
% it is possible that the two symbols have different types. In order to make these
% two types unifyable, the elaborator, tries to introduce a coerce on of the two
% type to the other. If not possible, it tries to enrich the one of the two object
% by finding a canonical structure.

% One example, which is at the earth of the \mc library, is the type called
% \eI{eqType}. In the code below we give a rather simplified version.

% \input{code/cs_struct.tex}

% The \eI{eqType} structure packs a type called \cI{obj} and \cI{eq} which is a
% function with type: \cI{~$\forall$~ e : eqType, obj e ~$\to$~ obj e ~$\to$~
% bool}. This function tests the boolean equality of its two inputs which should
% be instances of \cI{eqType}.

% \input{code/cs.tex}

% The canonical structure above inform the typecheck that it is possible to of
% \eI{op _ 3 4}, by inferring $e$ to \cI{eqNat}, which is the canonical structure
% for the \cI{nat} type.


% \paragraphTwo{Typeclasses vs Canonical structures}

% ``Find me a structure that works'' $\to$ typeclasses\\
% ``This type already carries its structure'' $\to$ canonical structures


\section{The \elpi language}
\label{sec:the-elpi-language}

\elpi~\cite{dunchev15} stands for \emph{Embeddable Lambda-Prolog Interpreter}.
It is a dialect of \lprolog~\cite{miller1991-pf,miller2012} that supports
higher-order logic programming and integrates a goal suspension mechanism
based on CHR (Constraint Handling Rules~\cite{chr,chr-elpi}).

The term ``embeddable'' highlights that \elpi\ is designed to be used as an
extension language. Its main application is the \rocqelpi plugin~(see
\cref{sec:coq-elpi}), which enables communication between \elpi\ as the
meta-language and the \rocq\ proof assistant as the object language.

A comprehensive presentation of the \elpi\ language and its applications can
be found in Tassi's ``habilitation à diriger les recherches''~\cite{hdr}.
Here we briefly introduce the aspects of \elpi\ that are relevant to this
document.

Sections~\cref{sec:elpi-syntax,sec:elpi-semantics} present the syntax and the
operational semantics of \elpi. A first formal description appeared in the
appendix of our contribution~\cite{fissore2026}, and was later revised
in~\cite{hdr}. In this thesis, we follow a slightly different presentation. The
goal is to be consistent with the conventions used in
\cref{chap:det-elpi,chap:mechanization}. We leave aside the syntax and semantics
of the \chr\ component of \elpi. This part is discussed only informally in
\cref{sec:chr}.

In \cref{sec:elpi-execution-example,sec:example-ho-dyn-programs}, we first
present an example of execution, highlighting the role of the \cut
operator in the \elpi language. We then discuss two main programming
styles in \elpi, namely higher-order programming and dynamic
programming, and explain how they interact with the \elpi semantics.

Finally, in \cref{sec:chr,sec:elpi-rule-names}, we outline two features of the
\elpi language: the constraint handling rules system, and rule naming
and grafting. 
% Finally, \cref{sec:ctx-elpi-related-words} reviews related
% work and compares our presentation of \elpi with existing approaches in
% the literature.

\subsection{Abstract syntax of \elpi (without \chr)}
\label{sec:elpi-syntax}

\begin{figure}
  \centering
  \includegraphics{img/syntax.pdf}
  \caption{Abstract syntax of \elpi\ programs}
  \label{fig:elpi-syntax}
\end{figure}

\paragraphTwo{Syntax of \elpi\ programs}

\Cref{fig:elpi-syntax} presents the abstract syntax of \elpi\ programs.

The basic syntactic categories are data~\DATA,
predicates~\PRED, unification variables~\UVAR, and
nominal variables~\NVAR. Nominal variables represent binders
introduced either by $\lambda$-abstractions or by $\pi$-quantification.

A term~\TERM\ is either a basic construct or an application. More precisely,
terms are formed by applying a term to a $\lambda$-term~\LTERM.
Lambda terms introduce abstractions; in the concrete \elpi\ syntax, an
abstraction is written \eI{x\ t}. Function application follows a curried
style. In contrast with \prolog, arguments are not passed within
parentheses, which allows partial application.

An formula~\ATOM\ represents an executable unit. It can be:
\begin{enumerate}
  \item the cut operator (\eI{!} in the concrete syntax),
  \item a predicate call (called atom in the literature),
  \item a $\pi$-quantification, written \eI{pi x\ t} in the concrete syntax,
        which introduces a fresh nominal variable \eI{x} visible in \eI{t}, or
  \item the dynamic insertion of a rule~\RULE, written \eI{r => t}, which
        makes the rule \eI{r} locally available while solving \eI{t}.
\end{enumerate}

A rule~\RULE\ consists of a head and a (possibly empty) body, which is a
list of atoms. The atoms in the body are called the premises of the rule.

For simplicity, we omit constraints from the syntax, since they
do not play an immediate role in the presentation. They are briefly
described in \cref{sec:chr}.

% The syntax we propose allows writing hereditary Harrop formulas. 
In this abstract syntax, logical connectives such as negation and disjunction,
which are usually encoded as native feature of a logic programming-language,
can be treated as first-class objects and implemented directly as \elpi rules.

% \begin{definition}[Atom polarity]
We say that a subformula appears either in a \emph{positive} or
\emph{negative} position within a formula~\cite{miller2012}.

\noindent
\begin{minipage}{0.75\textwidth}
A formula at the top level is in positive position. If a formula $f_1
\Rightarrow f_2$ is positive (resp. negative), then when crossing the
implication, the left-hand side is negative (resp. positive), while the
right-hand side is positive (resp. negative).

In the example on the right we explicitly annotate the positive/negative
occurrences of each subformula.
\end{minipage}
\begin{minipage}{0.25\textwidth}
\[
\underbrace{\underbrace{(\underbrace{x}_{pos} \Rightarrow
\underbrace{y}_{neg})}_{neg} \Rightarrow
\underbrace{z}_{pos}}_{pos}
\]  
\phantom{aa}
\end{minipage}

This notion of polarity will be used again in
\cref{sec:instance-encoding} when discussing the encoding of instances.

\paragraphTwo{Syntax of \elpi\ types}

In \elpi, users can define the algebraic data types required by their
development as well as the signatures of predicates. A new algebraic data type
is declared with the \kind\ keyword, while constructors of that type are
introduced using the \type\ keyword. A type is ether a
base type, a variable, or the application of a type to another in the
case of parametric types. The arrow symbol is used to type function space and in
\elpi it take an optional mode information. The mode, \Einput or
\Eoutput, tells the runtime which unification algorithm to use when unifying the
arguments of two terms, we dealy this discussion in the next paragraph. By
default, no mode information is compiled to \Eoutput.

For example, the following declarations define Church naturals and
polymorphic lists.

\mvspace
\begin{minipage}{0.45\textwidth}
  \input{code/elpi_nat.tex}
\end{minipage}
\begin{minipage}{0.5\textwidth}
  \input{code/elpi_list.tex}
\end{minipage}
\mvspace

The type \eI{nat} contains the constant \eI{z} representing zero and the
constructor \eI{s} representing the successor of another natural number.
A \eI{list} is either empty \eI{nil} or the
constructor \eI{cons} that combines a head element with a tail list.
Lists are polymorphic: the type of the head element must coincide with
the type of the elements stored in the tail.

A predicate is a symbol whose type ends in \eI{prop}. The type \eI{o}
is an equivalent notation following the convention of~\cite{miller2012}.
\elpi\ provides a dedicated syntax for declaring predicates, which also
allows the programmer to specify the modes of their arguments.

The declaration \eI{pred p t1 ~$\to$~ t2}, where \eI{t1} and \eI{t2} are
types, is equivalent to \eI{type p t1 ~$\to$~ t2 ~$\to$~ prop}. However, it also
defines the modes of the two arguments.

Arguments placed before the arrow symbol in a \eI{pred} declaration
are considered inputs, while those
after the arrow are outputs.

% \paragraphTwo{Program type checking}

% \elpi\ is an (almost) strongly typed language. When a program is
% interpreted, the type checker verifies that it is well typed. Any type
% inconsistency is reported as an error, which typically helps identify
% and correct implementation bugs early.

\subsection{Operational semantics of \elpi\ (without \chr)}
\label{sec:elpi-semantics}

\begin{figure}[t]
  \centering
  \includegraphics{img/det_check/dynamic.pdf}
  \caption{Operational semantics of \elpi}
  \label{fig:elpi-semantics}
\end{figure}

In thise section, we use the notation $\vecL{x}$ to represent an ordered list of
$x$ elements. The binary operator $+$, when applied to partial mappings
performs their disjoint union. The notation $t_1 + t_2$, when $t_1$ and $t_2$
are types, represent an object bying of type either $t_1$ or $t_2$, the notation
$t_1 + \mnone$ represent an object of type $t_1$ or a failure.
The notation \eI{set ~S~} represent a set, that is an unordered duplicate-free
collection of elements of type $S$.

A substitution $\sigma$ of type $\Sigma$ is a partial mapping from
unification variables \UVAR\ to terms \TERM. The empty substitution is denoted by
$\EmptySubst$. The notation $\sigma = \{ X \mapsto t\}$ represents a substitution
$\sigma$ where the variable $X$ is assigned to the term $t$.
The domain $\dom\ \sigma$ of a substitution $\sigma$ is the set
of variables that are assigned in $\sigma$.

\begin{definition}[Substitution extension]
  A substitution $\sigma'$ is an extension of $\sigma$, noted $\sigma \subseteq \sigma'$,
  iff
  $$\exists \sigma'',\ \sigma + \sigma'' = \sigma' \ \land\
  \dom\ \sigma'' \cap \dom\ \sigma = \varnothing $$
  \label{def:sub-extension}
\end{definition}

\vspace{-3em}

\begin{definition}[Substitution application (or term dereferencing)]
  A substitution $\sigma$ applied to a term $t$, noted $\sigma t$, is the operation
  allowing to recursively replace the variables in $t$ by their assignement in 
  $\sigma$.
  \label{def:deref}
\end{definition}

% \begin{definition}[Typing environment]
%   A typing environment is a partial mapping from symbol names to their type.
% \end{definition}

We use the notation $\env$ to represent a typing environment, it
maps symbol names to their type.

A program $\prog$ of type $\PROG$ is made by a triple
$(\vecL{\RULE}, \vecL{\NVAR}, \env)$

The first element represent the ordered list of rules characterising the
program, we use the notation $\PROG.\cI{index}$ to get this projection from a
program \prog. The second eleemnt of the triple contains the set of nominal
variables that have already been introduced, we use the notation
$\prog.\cI{nvar}$ to get this projection for the program \prog. Finally, the
last projection of the triple contain the typing environment, we use the
notation $\prog.\cI{sig}$ to get this projection from \prog.

We write $x \# \prog$ to denote that the nominal variable $x$ is fresh with
respect to $\prog.\txtI{nvar}$. The notation $x +_\forall \prog$ denotes the
program obtained by adding $x$ to $\prog.\txtI{nvar}$. Similarly,
$h +_\Rightarrow \prog$ denotes the program obtained by prepending the rule $h$
to $\prog.\txtI{index}$.

A goal $g$ of type
$\g = \PROG \times \ATOM \times \vecL{\alt}$
is a triple consisting of a program, an atom, and a list of
\emph{cut-to alternatives}. An alternative $a$ of type $\alt$ is a pair
$\Sigma \times \vecL{\g}$ composed of a substitution and a list of goals.

\Cref{fig:elpi-semantics} presents the derivation rules of the \elpi\
interpreter, defined in terms of the function
\[
  \runE : \vecL{\alt} \to (\Sigma, \vecL{\alt}) + \mnone .
\]

This semantics is inspired by
\cite[Sec.~4.3]{debray1988} and \cite{hdr}. Execution is modeled as the
evaluation of a stack of alternatives $\alt$. The interpreter processes
alternatives sequentially. The evaluation of the alternative list either succeeds,
producing a substitution and a new list of alternatives, or fails, returning
$\mnone$.

In \cref{fig:elpi-semantics}, rule \runE[\top] captures success: if the first
alternative contains an empty list of goals, the interpreter returns its
associated substitution together with the remaining alternatives. Conversely,
rule \runE[\bot] states that evaluation fails when the list of alternatives is
empty.

The remaining rules of \runE, namely \runE[!], \runE[\backchain],
\runE[\forall], and \runE[\Rightarrow], apply when the first alternative
contains a non-empty list of goals. Such alternative list has the form
$\ConsHd{\ConsHd{q}\ConsTl{g}}{\ConsTl{a}}$, where $q$ is the current goal atom
(or query). The derivation rule to use depends on the syntactic form of $q$, which
can be one of the four forms of \ATOM\ listed in \cref{fig:elpi-syntax}.

If $q$ is a \cut, rule \runE[!] discards the current alternatives $a$ and
replaces them with the cut-to alternatives stored in $q$. Evaluation then
continues with the remaining goals $g$.

If $q$ is a $\pi$-quantification $\pi x.\,t$, the interpreter generates a fresh
nominal variable $y$ not occurring in $\prog$, replaces all free occurrences of
$x$ in $t$ with $y$, and continues evaluation with the resulting atom.

If $q$ corresponds to the dynamic insertion of a rule $h$, evaluation continues
with atom $t$ in the program extended with the local rule $h$. Intuitively $h
\Rightarrow t$ amounts to a well-bracketed use of \prolog's \texttt{assert/1} and
\texttt{retract/1}.

Finally, if $q$ is a predicate call, the interpreter performs \emph{backchaining} over
the rules stored in $\prog.\txtI{index}$. Backchaining retrieves the rules that
can be used on the query $q$. In standard \prolog this is done by selecting
rules whose head unifies with $q$. In \elpi the process depends on predicate
modes, which determine if a argument should be ``matched'' or ``unified''.

\elpi provides two procedures, \unify\ and \matching. Both have type
\[
\TERM \to \TERM \to \Sigma \to \Sigma + \mnone ,
\]
taking two terms and an initial substitution $\sigma$. They either fail
(returning $\mnone$) or succeed with an updated substitution $\sigma'$ that is
an extension of $\sigma$.

\begin{definition}[Pattern fragment]
  A $\lambda$-term belongs to Miller's pattern frangment~\cite{miller1991-pf} if in every subterm of
  the form $F\ t_1\ t_2 \dots t_n$, where $F$ is a higher-order unification
  variable, each term $t_i$ is a bound variable distinct from any $t_j$ (with $i
  \neq j$).
  \label{def:pattern-fragment}
\end{definition}

The procedure \unify\ implements higher-order unification restricted to the
pattern fragment, and is used for arguments in \Eoutput\ position. The
procedure \matching, inspired by \bprolog's ``matching clauses''
\cite{zhou2012}, is used for arguments in \Einput\ position. It also performs
higher-order unification restricted to the pattern fragment.

\begin{definition}[\unify]\label{def:unify}
Two terms $q$ and $u$ \emph{unify} under a substitution $\sigma$ if there
exists a substitution $\sigma'$ extending $\sigma$ such that
$\sigma' q = \sigma' u$, and $\sigma'$ is a most general unifier for the two terms.
\end{definition}

\begin{definition}[\matching]\label{def:matching}
A query $q$ \emph{matches} a pattern $u$ under a substitution $\sigma$ if there
exists a substitution $\sigma'$ extending $\sigma$ such that
$\sigma' u = \sigma q$, where $\sigma'$ assigns at most the free variables
occurring in $u$.
\end{definition}

Backchaining is defined by the function \backchain:

\begin{definition}[Backchain, $\backchain : \PROG \times \txtI{set}\ \VAR \times \TERM \times \Sigma \to
\vecL{\Sigma \times \vecL{\ATOM}}$]
\[
\backchain(\prog, V, q, \sigma) =
\bigg[
(\sigma', b)\ \kIF\
\select(\getmode(\prog.\mathrm{sig}, q), q, h, \sigma) = \sigma' \neq \mnone
\ \bigg|\ 
(\clauseCmd{h}{}{b}) \in \text{fresh}(V, \prog.\mathrm{index})
\bigg].
\]
\end{definition}

Intuitively, \backchain selects the rules from the program \PROG 
that can be applied to the current
query $q$ under a substitution $\sigma$. The variables occurring in the program
rules are assumed to be disjoint from those in the current execution state. To
guarantee this, the program is refreshed using the function \texttt{fresh},
which renames its variables with respect to the set $V$.

For each applicable rule, \backchain returns the substitution obtained by
matching/unifying the rule head with $q$, together with the corresponding
rule body.

The definition relies on two auxiliary functions:
\begin{align}
  \getmode &: \env \to \TERM \to \TYPE + \mnone \\
  \select  &: (\TYPE + \mnone) \to \TERM \to \TERM \to
              \Sigma \to \Sigma + \mnone .
\end{align}

The function \getmode retrieves the signature of the predicate occurring in a
term. It returns the signature if the predicate is present in the environment,
and $\mnone$ otherwise.

The function \select tests whether the query $q$ matches/unifies the head $h$ of a rule
under an initial substitution $\sigma$. The predicate signature determines the
mode of each argument, which in turn selects the appropriate procedure:
\matching\ for \Einput\ arguments and \unify\ for \Eoutput\ arguments.
If all arguments satisfy the unification condition, \select returns the resulting substitution
$\sigma'$; otherwise it returns $\mnone$.

\begin{definition}[\elpi backchain, $\backchainE :
\PROG \times \TERM \times \Sigma \times \vecL{\alt} \times \vecL{\g}
\to \vecL{\alt}$]

\[
\backchainE(\prog, t, \sigma, a, g) =
\bigg[
\sigma',
(
  [(\prog, q, a) \mid q \in b] \atsign g
)
\ \bigg|\ 
(\sigma', b) \in \backchain(\prog, \mathrm{get\_fv}\ (t,g::a,\sigma), t, \sigma)
\bigg].
\]
\end{definition}

The function \backchainE, used by rule \runE[\backchain] in
\cref{fig:elpi-semantics}, builds a new list of alternatives from the result of
\backchain (get\_fv is the function retrieving the list of free variables in 
the term $t$, the list of alternatives $g::a$ and the substitution $\sigma$).
For each filtered rule, the atoms in the rule body become new
goals whose cut-to alternatives are $a$ and whose continuation is $g$. The
substitution associated with each alternative is the one returned by
\backchain.

\paragraphTwo{Comparison with Tassi's hdr}

\begin{table}
    \begin{subtable}{0.48\textwidth}
    \centering
    \begin{tabular}{ll}
      \runE rules & \texttt{run} rules\\
      \hline
      \runE[\top]        & $\texttt{run}_\top$ \\
      \runE[\bot]        & unreachable case \\
      \runE[\backchain]  & $\texttt{run}_{\circlearrowleft,\bot,\sigma,@}$ + IH \\
      \runE[!]           & $\texttt{run}_!$ and IH \\
      \runE[\forall]     & $\texttt{run}_\forall$ + IH \\
      \runE[\Rightarrow] & $\texttt{run}_\Rightarrow$ + IH \\
    \end{tabular} \\
    \vspace{10pt}
    \phantom{aa}\\
    \caption{$\runE \to \texttt{run}$}
    \label{table:run-run-proof-t1}
  \end{subtable}
  \begin{subtable}{0.48\textwidth}
    \centering
    \begin{tabular}{ll}
      \texttt{run} rules & \runE rules\\
      \hline
      $\texttt{run}_\top$             & \runE[\top] \\
      $\texttt{run}_\bot$             & \runE[\backchain] and \runE[\bot] \\
      $\texttt{run}_\circlearrowleft$ & \runE[\backchain] and IH \\
      $\texttt{run}_@$                & \runE[\backchain] and IH \\
      $\texttt{run}_\sigma$           & \runE[\backchain] and IH \\
      $\texttt{run}_!$                & \runE[!] and IH \\
      $\texttt{run}_\forall$          & \runE[\forall] and IH \\
      $\texttt{run}_\Rightarrow$      & \runE[\Rightarrow] and IH \\
    \end{tabular}
    \caption{$\texttt{run} \to \runE$}
    \label{table:run-run-proof-t2}
  \end{subtable}
  \caption{High-level proof schema of~\cref{th:run-run-equiv}}
  \label{table:run-run-proof}
\end{table}

In the previous subsections, we presented the \elpi language, including its
syntax and semantics. This formalization has been
developed in collaboration with Enrico Tassi. A comprehensive description of
\elpi can be found in his work \emph{hdr}~\cite{hdr}.

We highlight here the main differences between his presentation and the one
adopted in this section. First, in the syntax, we use binary application for
terms, instead of the $n$-ary application used in \emph{hdr}. Second, in the
semantics, programs carry not only the list of rules and nominal variables, but
also the signature of declared predicates. We also omit the formal treatment of
constraints, as it is not required for the remainder of this manuscript; a brief
informal explanation is sufficient for our purposes (see~\cref{sec:chr}).

Another difference concerns the definition of \runE. In Tassi's presentation,
\runE, called \texttt{run}, is defined as a procedure that takes two arguments:
the current alternative and the list of future alternatives. This choice
reflects closely the concrete implementation in \elpi, where execution starts
from a query, and a query is represented as an alternative. In contrast, we
define \runE with a single input, namely a list of alternatives, from which the
first element is extracted by pattern matching when needed. This design choice
is motivated by the mechanization in \rocq (see \cref{chap:mechanization}),
where we need to prove the equivalence between two semantics and to represent
the execution of \runE on an empty list of goals.
Despite these differences, as the theorem below claims, the two semantics are
equivalent when executed on non-empty lists of alternatives, modulo the
absence of constraints, which is forced to the empty list in the \texttt{run}
semantic (this is performed by the \txtI{map}ping of one list to the other
and putting the empty set, i.e. $\emptyset$, of constraints).

\begin{theorem}
  \begin{equation*}
    \begin{split}
      \forall\ a_0\ a_s\ r\ \sigma,\ & \txtI{let}\ (a_0', a_s') := \txtI{map}\ (\lambda x. (\emptyset, x))\ (a_0 :: a_s) \txtI{ in} \\ 
                             & \txtI{let}\ r' := \txtI{map}\ (\lambda x. (\emptyset, x))\ r \txtI{ in} \\
                             & \runE\ (a_0 :: a_s) \arr (\sigma, r') \leftrightarrow \texttt{run}\ a_0'\ a_s' \arr (\emptyset, \sigma, r')
    \end{split}
  \end{equation*}
  \label{th:run-run-equiv}
\end{theorem}

\begin{proof}
  The proof is relatively simple. It is based on two inductions, one for each
  direction of the $\leftrightarrow$ operator, respectively on \runE and
  \texttt{run}. The intuition is illustrated in
  \cref{table:run-run-proof}, where IH denotes the induction hypothesis.

  The first subtable corresponds to the left-hand side of the implication, while
  the second one corresponds to the right-hand side. In both cases, the goal is
  to relate the constructors of the two semantics.

  More precisely, we match each constructor of one semantics with the
  corresponding constructors of the other, and, since our semantics has fewer
  constructors, depending on the considered case, several constructors of
  \texttt{run} may be used on a single constructor of \runE. We also point out,
  that the \runE[\bot] case in \ref{table:run-run-proof-t1}, is unreachable
  since we start from an non-empty list of goals.
\end{proof}

\subsection{Example of program execution in \elpi with \cut}
\label{sec:elpi-execution-example}

We believe that the main difficulty in the semantics presented in the previous
section lies in the treatment of choice points and their interaction with the
\cut operator.

Several variants of the cut operator exist in the literature. The variant we
consider, used in \elpi, is the so-called \emph{hard cut} (see, for example, the
documentation of \sprolog~\cite{swi-prolog-cut}). When the \cI{backchain}
function returns a non-empty list of alternatives, the first alternative is
executed immediately, while the remaining ones are stored as choice points.

Within a selected alternative, premises are evaluated from left to right, each
atom being executed in sequence. When a \cut is encountered, it removes two
kinds of choice points: (i) those generated by \cI{backchain} for the current
predicate call, and (ii) those created during the evaluation of premises to the
left of the \cut.

As a running example, we consider the program shown in \cref{fig:progx}. For
clarity, \cref{ex-elpi} illustrates its execution, explicitly showing the
pointers to the cut-to alternatives. We only highlight the alternatives targeted
by the \cut operator, since the alternatives stored for predicate calls do not
play a role in the semantics discussed here.


\begin{figure}[t]
  \input{code/prog_non_det.tex}
  \caption{Running example of a \elpi program}
  \label{fig:progx}
\end{figure}

\begin{figure}
  \centering
  \begin{subfigure}{0.2\textwidth}
    \includegraphics[width=0.35\textwidth, keepaspectratio]{img/det_check_coq/exec_example1E/elpi0.pdf}
    \caption{Initial query}
    \label{ex-elpi0}
  \end{subfigure}
  \begin{subfigure}{0.4\textwidth}
    \includegraphics[width=\textwidth, keepaspectratio]{img/det_check_coq/exec_example1E/elpi1.pdf}
    \caption{Stack after call to \eI{g 0 R}}
    \label{ex-elpi1}
  \end{subfigure}
  ~\\~\\
  \begin{subfigure}{0.5\textwidth}
    \includegraphics[width=\textwidth, keepaspectratio]{img/det_check_coq/exec_example1E/elpi2.pdf}
    \caption{Stack after evaluating \cref{ex-elpi1}}
    \label{ex-elpi2}
  \end{subfigure}
  \begin{subfigure}{0.48\textwidth}
    \includegraphics[width=\textwidth, keepaspectratio]{img/det_check_coq/exec_example1E/elpi3.pdf}
    \caption{Stack after evaluating \cref{ex-elpi2}}
    \label{ex-elpi3}
  \end{subfigure}
  ~\\~\\
  \begin{subfigure}{0.48\textwidth}
    \includegraphics[width=\textwidth, keepaspectratio]{img/det_check_coq/exec_example1E/elpi4.pdf}
    \caption{Stack after evaluating \cref{ex-elpi3}}
    \label{ex-elpi4}
  \end{subfigure}
  \begin{subfigure}{0.45\textwidth}
    \includegraphics[width=\textwidth, keepaspectratio]{img/det_check_coq/exec_example1E/elpi5.pdf}
    \caption{Stack after evaluating \cref{ex-elpi4}}
    \label{ex-elpi5}
  \end{subfigure}
  \caption{Example of execution}
  \label{ex-elpi}
\end{figure}

% We have not drawn arrows coming out of call atoms since the interpreter
% ignores the cut-alternatives in this case. 
% The evolution
% of the stack in \cref{ex-elpi} mirrors the example in \cref{sec:elpi-execution-example}:
% we evaluate the first goal of the first alternative. 
% During backchaining,
% we add a pointer to the remaining alternatives to each new cut operator.

% If a cut is evaluated, we replace the remaining alternatives with the
% cut-to alternatives. For example, in \cref{ex-elpi5}, evaluating the cut
% discards the second and third elements in the stack. Backtracking is
% performed by simply consuming the first alternative from the stack, as shown
% in \cref{ex-elpi3}.

Consider the program in \cref{fig:progx} and the query
$\eI{g 0 R}$.
Both rules for \eI{g} apply to this query. Backchaining therefore returns the
following two alternatives:
\[
  [(\sigma_1, [\eI{r A B, f B C, !}]),\; (\sigma_2, [\eI{f D E, f E F}])]
\]
with
$
  \sigma_1 \coloneq \{\eI{A} \mapsto \eI{0}, \eI{C} \mapsto \eI{R}\}
$
and
$
  \sigma_2 \coloneq \{\eI{D} \mapsto \eI{0}, \eI{F} \mapsto \eI{R}\}
$.
For simplicity, we choose an example where variable refreshing plays no role/
% , so we do not discuss the set \FV{} any further.

Execution continues with the first premise of the first alternative, namely
\eI{r A B} under $\sigma_1$, i.e. \eI{r 0 B}. The remaining alternatives are
stored as choice points.
Backchaining \eI{r 0 B} returns
$
  [(\sigma_3, []), (\sigma_4, []), (\sigma_5, [\eI{!}])]
$
where
$
  \sigma_3 \coloneq \sigma_1 + \{\eI{B} \mapsto \eI{0}\}
$
;
$
  \sigma_4 \coloneq \sigma_1 + \{\eI{B} \mapsto \eI{1}\}
$
and
$
  \sigma_5 \coloneq \sigma_1 + \{\eI{B} \mapsto \eI{2}\}
$.

The next step of interpretation continues with \eI{f B C} under $\sigma_3$,
that is, \eI{f 0 R}, and leaves $[(\sigma_4, []), (\sigma_5, [\eI{!}])]$ as new choice points.
This time, backchaining fails (no rule for \eI{f} matches input $0$). We
therefore backtrack to the most recently introduced choice point and retry
\eI{f B C} under $\sigma_4$, i.e. \eI{f 1 R}. This succeeds with
$\sigma_6 \coloneq \sigma_4 + \{\eI{R} \mapsto \eI{2}\}$.

We now reach the cut. At this point there are still two unexplored
alternatives: one from the third rule for \eI{r} and one from the second rule
for \eI{g} (respectively, $\sigma_5$ and $\sigma_2$). Both are discarded,
because they were created when, or after, the first rule for \eI{g} was
selected. In contrast, a cut does not discard choice points created earlier;
in this example, there are none.
The final solution is:
$
  \{\eI{A} \mapsto \eI{0}, \eI{B} \mapsto \eI{1}, \eI{C} \mapsto \eI{R},
  \eI{R} \mapsto \eI{2}\}
$.

% In \cref{tree-sem,stack-sem}, we provide fully mechanized operational semantics for
% \prolog with cut. They differ in how they represent the ongoing computation,
% in particular how alternatives are stored and how they are pruned by cut.

% We propose two formal, operational semantics for a logic program with cut. 
% The two
% semantics are based on different syntaxes, the first syntax (called tree)
% exploits a tree-like structure and is ideal both to have a graphical view of its
% evolution while the tree is being interpreted and to prove lemmas over it. The
% second syntax, called elpi, is the \elpi syntax and has the advantage of
% reducing the computational cost of cutting and backtracking alternatives by
% using shared pointers. We aim to prove the equivalence of the two semantics
% together with some interesting lemmas of the cut behavior.

% \marginpar{spiegare notation for list}
% $\epsilon$ is the empty substitution.

% \subsection{Features of the \elpi language}
% \label{sec:elpi-features}

% \paragraphTwo{Higher-order programming}

% One of the feature of the \elpi language is the possibility to do higher-order
% programming. 

% \paragraphTwo{Dynamic programming}

% \elpi~\cite{dunchev15} is a dialect of $\lambda$prolog~\cite{miller1991-pf,miller1988}

% Teyjus:~\cite{teyjus,miller2012}

% Elf: \cite{elf}

% Twelf:\cite{twelf}

% Lambda prolog: \cite{qi2009}

% WAM: \cite{ait1991-wam}
\subsection{Higher-order, dynamic and meta- programs in \elpi}
\label{sec:example-ho-dyn-programs}

One of the main features of \elpi is the possibility to work with
higher-order and dynamic programs. In this section, we present some simple
examples illustrating these two aspects. These examples will be used later as a
reference when discussing determinacy checking in \elpi (see
\cref{chap:det-elpi}).

\paragraphTwo{Higher-order programming} A well-known higher-order predicate in \elpi is \eI{map}, whose
implementation is shown below.
%
\begin{elpicode}
pred map (pred A ~$\to$~ B), list A ~$\to$~ list B.
map _ [] [].
map F [X|XS] [Y|YS] :- F X Y, map F XS YS.
\end{elpicode}

This is a Prolog-style definition. The predicate takes a
higher-order argument \eI{F}, which is applied to each element of the input
list. In the base case, the empty list is mapped to the empty list. In the
recursive case, the head \eI{X} is transformed into \eI{Y} using \eI{F}, and the
predicate is then applied recursively to the tail \eI{XS}.

Another common higher-order construct in \elpi is the predicate
\eI{once}, defined as follows.
%
\begin{elpicode}
pred once (pred) ~$\to$~.
once P :- P, !.
\end{elpicode}

The predicate \eI{once} acts as a wrapper around a goal. It executes the goal
\eI{P} and then removes all remaining choice points. As a consequence, only the
first solution is returned. For example, the query \eI{once (r 0 X)}, where
\eI{r} is defined in \cref{fig:progx}, produces a single result, binding
\eI{X} to \eI{0}.

The behavior of the \cut operator depends on where it is placed. The
following snippets illustrate similar programs with different uses of \cut.

\mvspace
\noindent
\begin{minipage}{0.33\textwidth}
  \input{code/cut_p1}
\end{minipage}
\begin{minipage}{0.33\textwidth}
  \input{code/cut_p2}
\end{minipage}
\begin{minipage}{0.33\textwidth}
  \input{code/cut_p3}
\end{minipage}
\mvspace

The query \eI{p1 1 X} returns four solutions for \eI{X}, namely
\eI{0, 1, 2}, and \eI{3}. This is because no \cut is used, so all possible
solutions are explored by backtracking.

The query \eI{p2 1 X} returns only one solution, \eI{X = 0}. In this case,
after the first solution of \eI{r 0 X} is found, the \cut removes all choice
points, both for \eI{r} and for \eI{p2}.

Finally, the query \eI{p3 1 X} returns two solutions, \eI{0} and
\eI{3}. Here, the use of \eI{once} removes the choice points generated by
\eI{r}, but it does not affect the second rule for \eI{p3}.

\paragraphTwo{Dynamic programming}

The domain where \elpi really shines is the manipulation of higher-order data
represented via the so-called higher-order abstract syntax (or $\lambda$-tree syntax)~\cite{miller2018}
also called HOAS~\cite{pfenning1988}.
Such data is pervasive in interactive provers
such as \rocq. In the paradigmatic example below \eI{tm} is
the data type for $\lambda$-terms.
%
\begin{elpicode}
kind tm type.                       % datatype of $\lambda$-terms in HOAS form
type app tm ~$\to$~ tm ~$\to$~ tm.            %  - binary application
type lam (tm ~$\to$~ tm) ~$\to$~ tm.          %  - $\lambda$-abstraction
\end{elpicode}
%
In the HOAS encoding of syntax with binders we do not find a node for variables:
we re-use the notion of bound variable of the programming language to encode the
one of the syntax tree. At the level of types we see that the \eI{lam}
node carries an \elpi function over terms. It is by applying this function
that one can recover the body of the $\lambda$-abstraction.
A simple program manipulating higher-order data is the function
performing a deep copy of a term:
%
\begin{elpicode}
pred copy tm ~$\to$~ tm.
copy (app A B) (app C D) :- copy A C, copy B D.
copy (lam F) (lam G)     :- pi x\ copy x x => copy (F x) (G x).
\end{elpicode}
%
The copy of an application is built from the copies of the subterms. What is
more interesting is how one copies under a binder.
The ``\eI{pi x\ copy x x =>}'' instruction
explains how to copy the new constant \eI{x}.
Finally the code performs a recursive call over \eI{F x}:
by applying the \elpi function \eI{F} to
the fresh symbol \eI{x} one obtains the body of the $\lambda$-abstraction
where the bound variable is replaced
by the new symbol \eI{x}.

In \elpi, all predicates are \emph{dynamic}, or open, hence
the code of \eI{copy} can be
augmented with rules at runtime. 
% \todo{remark app, lam and each x are different}
% \todo{we want copy to be a understood as a function}
% Even so \eI{copy} is a function because no
% two rules can apply to the same input: \eI{lam} and \eI{app}
% are different constants, and each constant \eI{x} generated dynamically is
% guaranteed to be fresh by the \eI{pi} operator, hence
% each \eI{copy x x} rule is mutually exclusive with the other rules.

The \eI{copy} predicate, as given, implements the identity and may seem rather useless, 
but it is not! Since the program is open, one can add new rules for
\eI{copy} and change its behavior. For example the code below
computes the weak head normal form of a $\lambda$-term by contracting
the application of a $\lambda$-abstraction
to an argument and uses \eI{copy} to perform the substitution:

\begin{elpicode}
pred whd tm ~$\to$~ tm.
whd (app H A) R :- whd H (lam F), !,           % we fire the redex
  pi x\ copy x A => copy (F x) S, whd S R.     %   and we continue
whd X X.                                       % or we stop
\end{elpicode}

The argument \eI{A} is put in place of the (previously) bound variable in
\eI{F} by replacing that variable with \eI{x} and
by running \eI{copy} with the addition of a special rule
copying \eI{x} to \eI{A}.
Our static analysis understands this programming pattern
and is able to recognize that \eI{whd} is also a function.
In particular the first rule, if taken, discard the second rule (due to the cut)
and only calls functions.

\paragraphTwo{Meta-programming}

In \elpi, code is a first-class object, meaning that it can be inspected and
generated within the language itself. As an example, consider a program that
performs the fusion of two calls to \eI{map}~\cite{wadler1989}.

\input{code/fuse_pred.tex}

The predicate \eI{fuse} analyzes the syntax of the two programs. If both correspond to mapping a
list, with functions \eI{F} and \eI{G}, it produces a new program of the form
\eI{map (comp F G)}, where \eI{comp} denotes function composition.

% This approach can improve efficiency. For instance, instead of executing the
% sequence \eI{map F L1 L2, map G L2 L3}, which traverses the list twice, one can
% combine the two computations. By calling \eI{fuse (map F) (map G) H}, a new
% program \eI{H} is obtained. Then, \eI{H L1 L3} computes the same result with a
% single traversal of the list.

Meta-programming is widely used in \rocqelpi. It allows the
generation of new rules that can be stored on disk and reused in later
executions of the program.

\subsection{\chr: goal suspension}
\label{sec:chr}

Constraint Handling Rules (\chr, \cite{chr,chr15}) is a declarative,
rule-based language designed to manipulate a multiset of constraints. A
\chr program consists of rules that match patterns of constraints,
optionally guarded by conditions, and transform the current constraint
store by adding or removing elements. Operationally, execution proceeds
by repeatedly selecting applicable rules and applying them to the
constraint store. This model offers a flexible way to express
simplification, propagation, and their combination, known as
simpagation.

From a conceptual point of view, \chr can be seen as a rewriting system
over constraints, where rules describe how local configurations evolve.
In contrast to standard logic programming rules, which operate on a
single goal, \chr rules may inspect and rewrite several constraints at
once. This aspect is important when correctness depends on the
interaction between constraints, rather than on their independent
resolution. For this reason, \chr naturally supports a more global view
of computation.

In \elpi, \chr is integrated as an extension of the underlying
\lprolog engine. The motivation for this integration comes from the need
to combine and refine suspended constraints generated during proof
search.

We do not enter into the details of the syntax or the implementation of
\chr, since they play only a minor role in this manuscript. A more
complete presentation can be found in \cite[Sec.~4.3]{chr-elpi} and
\cite[Sec.~2.4]{hdr}.

% In \elpi, \chr rules are considered in a given order and the
% execution commits to the first rule that matches and fires. This choice
% gives a predictable and efficient execution strategy, while still
% preserving the declarative reading of rules. Moreover, constraints are
% matched modulo a freezing mechanism: unification variables occurring in
% constraints are temporarily treated as rigid symbols, preventing
% undesired interactions between unrelated constraints.

% From the usage perspective, \chr in \elpi acts as a meta-level reasoning
% mechanism. While the core logic programming engine constructs proof
% trees in a depth-first manner, \chr rules operate on the set of suspended
% goals, thus affecting all branches simultaneously. This allows one to
% encode global invariants, such as uniqueness conditions or consistency
% checks, in a concise and declarative way. In practice, this combination
% of \lprolog with \chr makes \elpi particularly suitable for tasks
% such as type inference, elaboration, and manipulation of partially
% specified terms, where constraints must be accumulated, combined, and
% eventually resolved.

\subsection{Rule names and insertion}
\label{sec:elpi-rule-names}

In the \elpi database, the rules used during backchaining are stored in
an ordered data structure. Each rule
can optionally be given a name by the user.

These names are useful when adding new rules that should have higher or
lower priority than existing ones. In this way, the user can control the
position of rules in the search order.

In \elpi, inserting a rule before or after an existing one is called
\emph{grafting}.

\begin{elpicode}
  :name "nth-fail"
  nth _ _ :- error "The list is too short".
\end{elpicode}

The definition of \eI{nth} above is a catch-all rule, since it applies to
any query. This rule is named \eI{nth-fail}.
When defining additional rules for \eI{nth}, one can write:

\begin{elpicode}
  :before "nth-fail"
  nth 0 [X|_] X.
\end{elpicode}

This declaration places the new rule just before the catch-all rule. A
similar syntax can be used with \eI{:after} to insert a rule after an
existing one.

The usefulness of grafting becomes clearer in
\cref{sec:rule-prio}, where typeclass instances must be inserted at
specific positions in the database to respect the intended priority.


% \section{\rocqelpi: \rocq and \elpi interleaving}
\section{\rocqelpi: An \elpi plugin for \rocq}
\label{sec:coq-elpi}

The \rocqelpi language is a plugin for \rocq that enables the use of \elpi
within the \rocq environment. In the following sections, we first explain the
idea behind encoding \rocq terms in \elpi (see \cref{sec:hoas}) using
higher-order abstract syntax (HOAS).
In~\cref{sec:elpi-main-API}, we provide some important API that are widely used
in \rocqelpi. Finally, in \cref{sec:elpi-commands-tactics}, we provide a brief
description of how commands and tactics are written in \rocqelpi and how they
integrate with \rocq.

\subsection{HOAS: \texorpdfstring{$\lambda$}{lambda}-terms representation}
\label{sec:hoas}

In \cref{sec:example-ho-dyn-programs}, we sketched how to encode the syntax of a
language using the HOAS of \elpi. This syntax is inspired by the
$\lambda$-calculus, which forms the core of the \rocq system. 

The encoding of \rocq terms is performed using a \elpi type called
\eI{term}. The snippet depicted in \cref{fig:hoas} shows the main constructors
for terms.

\begin{figure}  
\begin{elpicode}
kind term type.

type sort    sort ~$\to$~ term.                 % Prop, Type@{i}
type global  gref ~$\to$~ term.

type app     list term ~$\to$~ term.            % app [hd|args]
type fun     name ~$\to$~ term ~$\to$~ (term ~$\to$~ term) ~$\to$~ term. 
type prod    name ~$\to$~ term ~$\to$~ (term ~$\to$~ term) ~$\to$~ term. 

...                     % Other constructors omitted
\end{elpicode}
\vspace{-1.5em}
\caption{HOAS of \rocq terms in \elpi}
\label{fig:hoas}
\end{figure}

The base constructors are \eI{sort}, which encodes the \cI{Type} and \cI{Prop}
terms of \rocq, and \eI{global}. The \eI{global} constructor represents
user-defined symbols such as types, inductives, and constants. 

These symbols are identified by a \eI{gref}, which is an opaque object
containing the unique identifier of the corresponding \rocq symbol. For better
space management we use a more compact notation: opaque \rocq{} symbols are
written using guillemets, avoiding the explicit use of the \eI{global}
constructor. For instance, the \rocq{} constant \cI{nat} is represented in HOAS
form as \cI{~<<nat>>~}.

Term application in \rocq is represented by the \eI{app} constructor,
which takes a list of terms. $\lambda$-abstractions and
$\forall$-quantifications are encoded using the \eI{fun} and \eI{prod}
constructors, respectively. Each of these constructors carries the
\eI{name} of the \rocq binder (used only for pretty printing), the type of the
binder, and an \elpi higher-order object of type
\eI{term ~$\to$~ term}, which represents the abstraction of the bound
variable in the term.

\begin{table}
\begin{tabular}{lll}
  \rocq terms & \elpi HOAS & description\\
  \hline
  \cI{plus 0}             & \eI{app [~<<~plus~>>~, ~<<0>>~]} & term application\\
  \cI{fun x => ?F a}       & \eI{fun `x` ~<<int>>~ y\ app [G, ~<<a>>~]} & $\lambda$-abstraction\\
  \cI{~$\forall$~ x, p x} & \eI{prod `x` ~<<bool>>~ z\ app [~<<p>>~, z]} & $\forall$-quantif and binding
\end{tabular}
\caption{Examples of \rocq terms encoded in \elpi using HOAS}
\label{tab:rocq-to-elpi-hoas}
\end{table}

The \rocqelpi backend translates \rocq terms into HOAS \elpi terms and
conversely when the two systems interact. In
\cref{tab:rocq-to-elpi-hoas}, we show some examples of this encoding:
the first column contains \rocq terms, and the second column their HOAS
representation in \elpi.

The second and third rows illustrate how binders are handled for
$\lambda$-abstraction and $\forall$-quantification. The original binder
name from \rocq is stored in the \eI{fun} and \eI{prod} constructors,
while the name of the bound variable in \elpi is not relevant. In the third row,
the \elpi binder (called \eI{z}) is reused to represent the abstraction
of the object language when applied to the predicate \eI{p}.

The second row shows a \rocq unification variable \cI{?F}. It is
translated into an \elpi unification variable \eI{G}, which internally
corresponds to \cI{?F}. When \eI{G} is instantiated during the execution
of the \elpi program, the result is translated back into a \rocq term and
assigned to \cI{?F}.

\paragraphTwo{Quotations and Antiquotations}

Writing \rocq{} terms can be tedious, as it requires explicitly constructing
their full HOAS representation. As shown in
\cref{tab:rocq-to-elpi-hoas}, the \elpi{} HOAS syntax is significantly more
verbose than standard \rocq{} syntax. Moreover, \rocq{} often includes implicit
subterms, whereas \elpi{} does not provide such a mechanism.

To simplify development, \rocqelpi{} offers a system of quotations and
antiquotations. This mechanism allows embedding \rocq{} syntax within \elpi{}
code, and conversely \elpi{} syntax within \rocq{} code, in a recursive manner.

An illustrative example, adapted from~\cite{hdr}, is the following:
%
$$\underbrace{\eI{print}}_{\elpi}\ \{\{ \underbrace{\cI{1 +}}_{\rocq}\ \eI{lp:}\{\{\underbrace{\eI{app[~<<S>>~, }}_{\elpi}\{\{ \underbrace{\cI{0}}_{\rocq} \}\} \underbrace{\phantom{aa}}_{\elpi} \}\} \underbrace{\phantom{aa}}_{\rocq} \}\}$$

Within an \elpi{} code block, the syntax \cI{{{ ... }}} denotes a
quotation that allows writing \rocq{} code. Conversely, within \rocq{} code, the
syntax \eI{lp:{{ ... }}} denotes a quotation for embedding \elpi{} code.
These constructs can be nested arbitrarily.

\subsection{API in \rocqelpi}
\label{sec:elpi-main-API}

Thanks to the HOAS representation of \rocq terms in \elpi, the two
languages become closer and can share a common representation. This
makes their interaction easier and allows interleaving computations
between them. The \rocq language exposes several APIs that can be used
directly from \elpi.
%
\begin{center}
  \eI{pred coq.unify-eq term, term ~$\to$~ diagnostic.}
\end{center}

The predicate above is widely used in \elpi developments. It takes two
terms and attempts to unify them. If unification fails, it returns a
diagnostic explaining the error. Internally, this predicate relies on
the \rocq unification algorithm. When \elpi{coq.unify-eq} is called, the
HOAS representation of the terms is translated into \rocq terms before
performing the unification.

% Other predicates allow extending the \rocq system with new definitions,
% modules, modes, and more.
% %
% \begin{center}
%   \eI{pred coq.TC.declare-class gref ~$\to$~.}
% \end{center}

% For example, the predicate above allows declaring a type class from a
% global reference in \rocq directly within \rocqelpi.

\subsection{Declaring Databases in \rocqelpi}

In \cite[Sec. 4.4.3]{hdr}, a database is defined as a non-executable \elpi
program. It contain a collection of code that can be included into commands
or tactics by referring to its name.

When a program is executed, it does not use the state of the database at the
moment when the command was defined. Rather, it accesses the current content
of the database at execution time. This means that any update to the database
is visible to all programs that include it.

An \elpi database can be dynamically modified by adding new predicate
signatures and rules.

\begin{elpicode}
kind clause type.
%           Name     Grafting    Rule
type clause string ~$\to$~ grafting ~$\to$~ prop ~$\to$~ clause.

%                           DB name  Indexing  PName
pred coq.elpi.add-predicate string, string, string,
                                  list (pair argument_mode string) ~$\to$~.
%                        Scope    DB name   Clause
pred coq.elpi.accumulate scope, string, clause ~$\to$~.
\end{elpicode}

The \eI{coq.elpi.add-predicate} predicate takes as arguments the name of
the database where the predicate is inserted, the indexing
strategy\footnote{We do not discuss indexing in detail, as it is not
central for this manuscript. It determines the data structure used to
store rules in order to improve search efficiency.}, the predicate
name, and its specification (argument modes and types).

The \eI{coq.elpi.accumulate} predicate adds a new clause to a database
within a given \rocq scope (e.g. module, section, file \dots). A clause is a specific type with a single
constructor, which contains the clause name, its grafting with respect
to the existing database, and the rule itself.

These last two predicates are particularly useful in the context of
\cref{sec:class-encoding,sec:instance-encoding}.

\subsection{Writing Commands and Tactics in \rocqelpi}
\label{sec:elpi-commands-tactics}

\begin{table}[b]
  \begin{tabular}{lll}
    Commands                        & Tactic                        & Description \\
    \hline
    \cI{Elpi Command command_name.} & \cI{Elpi Tactic tactic_name.} & Declaration \\
    \cI{Elpi command_name.}         & \cI{elpi tactic_name.}        & Invocation \\
    \eI{main}                       & \eI{msolve} and \eI{solve}    & Entry point
  \end{tabular}
  \caption{Command and Tactic main commands}
  \label{tab:comm-tac}
\end{table}

We now describe how to implement commands and tactics in \rocqelpi. In
\cref{tab:comm-tac}, we summarize how to define commands and tactics, how to
invoke them, and what their entry points are. Both commands and tactics are
typically built on top of an \elpi database, which they can read and, if needed,
modify. A database is added using the command \cI{Elpi Accumulate Db db_name}.
The code of commands and tactics can be extended as follows:

\begin{coqcode}
Elpi Accumulate [command_name|tactic_name] lp:{{
  ~\PYG{c}{\PYGZpc{} New rules added here}~
}}.
\end{coqcode}

As mentioned at the beginning of this chapter, commands are used to extend the
\rocq language with new definitions and theorems. In other words, a command does
not return a value: it is mainly used for printing or for modifying the internal
state of the system. The entry point of a command is a predicate named
\eI{main}, with signature \eI{pred main list argument ~$\to$~}. An argument can be a
string, an integer, a term, or an open term.

Tactics in \elpi are used to solve \rocq goals through the \elpi interpreter.
Their declaration is similar to that of commands. The entry point of a tactic is
the predicate \eI{msolve}, with the following type:
%
\begin{center}
  \eI{pred msolve list sealed-goal ~$\to$~ list sealed-goal.}
\end{center}

\begin{figure}
\begin{elpicode}
kind goal type.
kind sealed-goal type.
type nabla (term ~$\to$~ sealed-goal) ~$\to$~ sealed-goal.
type seal  goal ~$\to$~ sealed-goal.

typeabbrev goal-ctx (list prop).
%          Context    RS     Ty      Sol     Arguments
type goal  goal-ctx ~$\to$~ term ~$\to$~ term ~$\to$~ term ~$\to$~
           list argument ~$\to$~ goal.
\end{elpicode}
\vspace{-1.5em}
\caption{Representation of goals and sealed goals in \rocqelpi}
\label{fig:goals-type}
\end{figure}

The \eI{msolve} predicate takes as input a list of \rocq sealed goals and
returns an updated list of sealed goals. The type \eI{sealed-goal}, shown in
\cref{fig:goals-type}, represents goals that may contain abstractions. These
abstractions are encoded using the \eI{nabla} constructor.

Under a spine of \eI{nabla} abstractions, the \eI{seal} constructor contains a
goal. A goal is a tuple with five components: a context $C$, a raw solution
$S_r$, a type $T_y$, a final solution $S$, and a list of arguments $L$.

The context $C$ maps quantified variables to their types. The term $S_r$ is a trigger:
when it is instantiated, it is elaborated against the type $T_y$. The type $T_y$
is the type of the \rocq goal to be solved. The term $S$ is the final solution,
which must have type $T_y$. The list $L$ contains arguments that control the
behavior of the resolution process. In this work, we mainly use two projections
of goals, namely the context and the goal type.

If the predicate \eI{msolve L N} is not defined, or if it fails when applied to
the list of goals \eI{L}, then the system attempts to solve each goal in
\eI{L} individually using the predicate \eI{solve}. In this case, \eI{solve} is
called on the content of the \eI{seal} constructor, meaning that all
\eI{nabla}s have already been traversed, and the type of these nablas loaded in
the context as local rules with \eI{=>}.
%
\begin{center}
  \eI{pred solve goal ~$\to$~ list sealed-goal.}
\end{center}

The final point addressed in this section concerns the \eI{refine} predicate.

Given a goal \eI{G} and a proof term \eI{P}, one may wish to typecheck \eI{P}
against the \eI{G} and obtain the corresponding list of subgoals that must be
solved recursively. The \rocq tactics performing this task is called
\eI{refine}.

In \elpi{}, the \eI{refine} predicate exists and has the same behavior: it takes
a proof term and a goal, it typechecks the proof agains the goals and returns a
list of subgoals on the for of a list of sealed-goals. It is customary for
\elpi{} tactics to invoke \eI{refine} in order to produce the output list for the
\eI{solve} predicate.

For performance reasons, \elpi{} also provides a variant, \eI{refine.no_check},
which skips typechecking and directly produces the list of sealed subgoals from
the given term and goal. This results in faster, although less reliable,
computation.

% \subsection{Concluding remarks}

% Overall, \elpi is good for elaboration
 
% \section{Notational conventions}
% In the following section we note $\mathbb{B}$ for the boolean type, and $\top$
% and $\bot$ for \cI{true} and \cI{false}. The constructors of the option type are written
% $\msome{t}$ for \cI{Some}$~t$ and $\mnone$ for \cI{None}.
% Following the Mathematical Components library, on which we depend,
% we use \cI{x.1} and \cI{x.2} to denote the first and second projection
% applied to the pair \cI{x}. List concatenation is the infix \cI{++},
% while list comprehension (i.e. mapping a function on a list) is written
% \cI{[seq f x | x <- l]}. The function \cI{odflt d o}
% extracts the value from the option \cI{o} and returns the default \cI{d}
% if \cI{o} is $\mnone$.
