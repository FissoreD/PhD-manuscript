\chapter{Architecture of a Type-Class Solver in \rocqelpi}

Type classes provide an elegant mechanism for implementing ad-hoc
polymorphism~\cite{wadler1989}. They allow the same symbol to be reused in
different contexts while preserving the same mathematical properties. From a
Prolog-like perspective, a type-class problem corresponds to resolving a query
to a predicate that associates its input with an instance solving the
type-class constraint. In \rocq, instances act as rules stored in a database,
which are used to backchain and resolve type-class queries.

To address type-class unification problems, we extended \rocq with an API called
\cI{Class_tactics.register_solver}. This API allows developers to register
custom type-class solvers within the \rocq elaborator. In this way, type-class
resolution can be delegated to a meta-language: the custom solver computes a
solution (if it exists) to the type-class constraint and returns it to the
\rocq elaborator. We refer to such a custom solver as a \emph{meta-solver}.

% When the elaborator encounters a type-class constraint during the elaboration
% of a \rocq term, it may delegate its resolution to a registered meta-solver.
% The meta-solver receives the type-class goal together with the context in
% which it appears and must return an updated environment (called an
% \cI{evar_map} in \rocq terminology).

To support this mechanism, we also introduced another API,
\cI{Classes.register_observer}. This API provides a systematic way to notify
the meta-language whenever a new class or instance is declared in \rocq. The
meta-language can then translate these declarations into its own
representation and store them in its knowledge base.

Concretely, a meta-language integration must provide two components:
(1) a compiler that translates \rocq class and instance declarations into the
meta-language representation, and (2) a type-class solver that animates the
search by interacting with the resulting database.

In our setting, the meta-language used to implement the meta-solver is
\elpi. Consequently, the main task is to implement the class and instance
compiler, which incrementally builds the database on which the \elpi program
operates.

The type-class solver itself typically follows a \prolog-like resolution
strategy: instances are tried according to their priority, and backtracking
may reconsider previous choices when a branch fails. In \elpi, this behavior
comes for free, since the resolution mechanism of the type-class solver
strongly imitates the standard execution model of the \elpi runtime.

In the following sections
(\cref{sec:class-encoding,sec:instance-encoding,sec:goal-encoding}), we
present the compiler by separating its three main components: the class
compiler, the instance compiler, and the goal compiler.

In \cref{sec:rule-indexing}, we describe our work on an alternative rule
indexing mechanism for the \elpi runtime, designed to speed up clause
retrieval during resolution.

Finally, in \cref{sec:tc-discussion}, we discuss practical aspects of using
the type-class solver. In particular, we explore how users can control and
customize the solver to better fit their needs. We also show the
application of our solver to existing libraries, such as \tlc, \stdpp, and
\iris.

\section{Simple instance compiler}
\label{sec:simple-instance-compiler}

A type-class goal is a \rocq type\footnote{or term, since in \rocq\ types are
terms.} $T$ whose head symbol is a type-class $C$. Solving such a goal amounts
to finding a \rocq instance whose type is an instantiation of $T$.

Conceptually, a type-class goal can be translated into a query with two
arguments: the type to be solved and the concrete instance that provides the
solution. The predicate used for the search is \eI{pred tc term -> term}.

As an example, consider the \cI{Add} type-class introduced in
\cref{sec:ad-hoc-poly}. For convenience, the full definition of the class and
its instances is reproduced in \cref{fig:add-class-full}.
%
\begin{figure}
  \input{code/plus_tc_add_inst.tex}
  \caption{Add class and instances}
  \label{fig:add-class-full}
\end{figure}
\begin{figure}
  \input{code/toy_compiler_tc.tex}
  \caption{Simple \elpi compiler}
  \label{fig:simple-elpi-compiler-tc}
\end{figure}

After an instance is declared, an observer triggers the instance compiler
shown in \cref{fig:simple-elpi-compiler-tc}. The compiler is similar to the
example presented in \cite[Sec.~4.5]{hdr}.

The entry point of the compiler is the predicate \eI{compile}, which takes the
global reference \eI{G} of the instance as input. Using the
\eI{coq.env.typeof} API, the compiler retrieves the type \eI{Ty} of \eI{G}.
Compilation then proceeds by invoking the auxiliary predicate \eI{comp} to
produce the rule \eI{R}. Finally, \eI{coq.elpi.accumulate} adds the clause
\eI{R} to the database, here called \eI{tc.db}.

The predicate \eI{comp} performs the core of the compilation. It takes four
arguments: the instance term; its type, which is structurally consumed during
recursion; and two accumulators storing the list of instance arguments and the
list of premises that will form the body of the resulting rule. From these
inputs, \eI{comp} produces the \elpi rule corresponding to the compiled
instance.

Compilation proceeds by analyzing the structure of the instance type. Three
cases are relevant. Rules \eI{~\PYG{c}{r$\to$}~} and
\eI{~\PYG{c}{r$\forall$}~} handle quantifications, while rule
\eI{~\PYG{c}{rB}~} is the base case that constructs the final \elpi rule from
the information accumulated during the recursion.

The base case is reached when the current type \eI{Ty} is not a quantification.
The compiler then constructs the final rule by assembling its head and body.

The head is the predicate \eI{tc} applied to the instance type \eI{T} and to a
proof term \eI{Proof}. The proof is obtained by applying the instance \eI{I} to
its arguments \eI{rev Ag}. Since the arguments are accumulated in reverse order
during the traversal, the list \eI{Ag} is reversed before building the
application.

The premises of the clause are stored in the accumulator \eI{P}. As for the
arguments, the list is reversed before constructing the final rule.

For example, compiling the instance \cI{addNat} produces the rule

\begin{center}
  \eI{tc {{Add nat}} {{addNat}}}.
\end{center}

When the current type \eI{Ty} is a quantification, the compiler introduces a
fresh nominal variable representing the bound variable in the recursive call.
Two situations must be distinguished depending on the shape of the
quantification.

If the quantification corresponds to a premise of the instance, rule
\eI{~\PYG{c}{r$\to$}~} is used. This rule introduces a fresh variable \eI{x}
representing the conditional instance with type \eI{T}. The premise
\eI{tc T x} is added to the list of premises \eI{P}, and the variable \eI{x} is
added to the list of instance arguments \eI{Ag}.

Rule \eI{~\PYG{c}{r$\forall$}~} handles dependent quantification corresponding
to parameters of the instance, that are supposed not to have a class as type. In
this case the abstraction is simply consumed: the list of premises \eI{P}
remains unchanged, while the variable \eI{x} is added to the list of instance
arguments.

Both rules \eI{~\PYG{c}{r$\to$}~} and \eI{~\PYG{c}{r$\forall$}~} return a term
of the form \eI{(pi y\ R y)}, where \eI{R} is the result of the recursive call
to \eI{comp}. Since \eI{R} has type \eI{term -> prop} and abstracts the local
variable \eI{x}, the compiler introduces a fresh variable \eI{y} to quantify
over the corresponding argument. This ensures that the resulting rule is
closed (ground), as required by the \eI{coq.elpi.accumulate} predicate.

As an example, consider the instance \eI{addProd}, whose type is

$$
\forall\ \underbrace{x\ y}_{\eI{~\PYG{c}{r$\forall$}~}},
\underbrace{\cI{Add}\ x \to \cI{Add}\ y}_{\eI{~\PYG{c}{r$\to$}~}} \to
\underbrace{\cI{Add}\ (x * y)}_{rB}.
$$

Compiling this instance yields the following \elpi rule:

\begin{elpicode}
  pi L R PL PR\ 
    tc {{Add (lp:L * lp:R)}} {{addNat lp:L lp:R lp:PL lp:PR}} :-
      tc {{Add lp:L}} PL, tc {{Add lp:R}} PR.
\end{elpicode}

This rule provides a proof for \eI{Add (x * y)} provided that the two premises
\eI{Add x} and \eI{Add y} can be solved.

To also solve type-class goals, we define an \elpi tactic containing the
following rule:

\begin{center}
  \input{code/toy_compiler_tc_solver.tex}
\end{center}

Given a \rocq goal \eI{G}, the tactic retrieves its type \eI{Ty}, performs a
backchaining step on \eI{tc Ty P} to obtain a proof \eI{P}, and refines the goal
\eI{G} with \eI{P}. The refinement produces a list of subgoals \eI{S}, which are
returned to \rocq.

The full code, together with a working example, can be found in
\cref{app:full-simpl-compiler}.

The implementation presented in this section illustrates the expressive power of
the \elpi language: the core of the compiler fits in roughly eight lines of
code, while a similar number of lines is required to complete the \elpi command
fot the compiler. The solver itself consists of a single rule. However, this
simplified implementation has some limitations it is worth to point out.

\begin{itemize}
  \item The predicate \eI{tc} does not expose the class arguments as \elpi
        arguments. Consequently, the modes declared on a class are not preserved
        in the \elpi solver. The compiler should therefore generate a specialized
        predicate for each class, with arguments and modes matching those of the
        class so that rule backchaining can be performed correctly. This problem
        is addressed in \cref{sec:class-encoding}.
  \item Rule $r\forall$ does not account for the possibility that the quantified
        variable $x$ is itself a premise of the class. In complex instances,
        $x$ may have a class as its type and should therefore appear as a
        premise in the generated rule. This problem is addressed in
        \cref{sec:instance-encoding}.
  \item The compiler does not handle instances of the form
      $(c_1 \to c_2) \to c_3$, where the implementation of $c_3$ requires a
      proof of $c_2$ in a context extended with a local instance of $c_1$.
      Supporting such cases requires compiling deep hereditary Harrop
      formulas and therefore tracking the polarity of the instance being
      compiled. This problem is addressed in
      \cref{sec:instance-encoding}.
  \item The \eI{solve} rule of the solver ignores the context of the goal. As
        discussed in \cref{sec:elpi-tactics}, the first projection of a
        \eI{goal} in \rocqelpi is its context, which may contain local
        instances that should be compiled and used during resolution. This
        problem is addressed in \cref{sec:goal-encoding}.
\end{itemize}

\section{Class compilation}
\label{sec:class-encoding}

When a class $c$ is declared in \rocq, the compiler generates a corresponding
\elpi predicate. If the class has $n$ parameters, the generated predicate has
$n+1$ arguments: the first $n$ arguments correspond to the parameters of $c$,
while the final argument represents the instance witnessing the class in the
current rule.

From the perspective of the \elpi solver, two aspects of a class declaration in
\rocq are particularly relevant: the global reference identifying the class and
the modes associated with its parameters. In \rocq, users may explicitly
declare modes using the attribute \cI{~\#~[mode="..."]}. If no mode declaration
is provided, the parameters of the class are considered outputs by default.

The compiler responsible for generating the corresponding \elpi predicate is
shown in \cref{fig:class-compiler}.

\begin{figure}
  \input{code/toy_compiler_pred.tex}
  \caption{Class compiler}
  \label{fig:class-compiler}
\end{figure}

The entry point of the compiler is the predicate \eI{add-class-pred}, which
takes two arguments: the global reference \eI{C} of the class and a string
\eI{S} describing its modes. The string \eI{S} encodes the modes using the
three \rocq symbols \cI{-, +, !}, separated by spaces. If the user does not
provide an explicit mode declaration, \eI{S} is the empty string.

Predicates can be created dynamically in \elpi using the API
\eI{coq.elpi.add-predicate}. This primitive takes four arguments: the database
in which the predicate will be stored (in our case \eI{tc.db}), the indexing
strategy, the predicate name \eI{N}, and a list \eI{M} of values of type
\eI{mode-type}. Each element of \eI{M} specifies the mode and the type of one
argument of the predicate. The predicate name is constructed from the global
reference of the class using the predicate \eI{gref->pred-name}. The list of
modes \eI{M} is obtained by processing the string \eI{S} with the predicate
\eI{str->modes}.

Two cases must be distinguished depending on whether the string \eI{S} is
empty. If \eI{S} is empty, the compiler derives the modes from the type of the
class. It retrieves the type of \eI{C} and creates a pair \eI{pr out "term"} for
each quantification in that type. In the base case, an additional output
argument corresponding to the instance is added, again represented by the pair
\eI{pr out "term"}.

As an example, consider the declaration of the class \cI{Add}
(cf.~\cref{fig:add-class-full}). When this class is declared, the \elpi class
compiler is triggered and receives the \rocq symbol \eI{indt «Add»} as input.
Since no mode attribute is provided, the compiler receives the empty string.

From the symbol \cI{Add} the compiler retrieves its type and determines the
number of parameters. In this example, the HOAS representation of the type of
\cI{Add} in \elpi is
%
\begin{center}
  \eI{prod `T` (sort (typ «Add.u0»)) c0 \ sort (typ «Add.u0»)}
\end{center}

\noindent which indicates that the class has a single parameter.

From this information the compiler generates the predicate associated with the
class:

\begin{center}
  \eI{pred tc-Add o:term, o:term.}
\end{center}

If \eI{S} is not empty, the user has provided explicit modes for the class. In
this case, each \rocq mode is translated into the corresponding \elpi mode.
This translation is performed by the predicate \eI{str->mode}, which maps
\cI{-} to the \elpi output mode, and both \cI{+} and \cI{!} to the \elpi input
mode.

\subsection{\rocq and \elpi mode discrepancies}
\label{sec:tc-mode-and-determinacy}

Our typeclass solver is not intended to replicate the \rocq solver. Instead, it\marginpar{posto giusto senza aver discusso istanze?}
aims to exploit, whenever possible, the features of the \elpi language. A
natural claim is that mapping two \rocq mode to only one in \elpi is that the
\rocq and \elpi type-class solvers will have different runtime behaviors.

We can note that there is no direct correspondence between \rocq modes and
\elpi modes. While the \rocq mode \cI{-} corresponds naturally to the \elpi
output mode, neither \cI{+} nor \cI{!} has a precise counterpart among the
\elpi input mode. Furthermore, a \rocq class may declare multiple mode
specifications, whereas an \elpi predicate admits only a single mode
declaration.

We recall that the \rocq modes \cI{+} and \cI{!} impose conditions on the shape
of the argument: \cI{+} requires a ground term, while \cI{!} requires a term
with a rigid applicative head. During the resolution phase, if a goal does not
satisfy the mode constraint imposed by the class declaration, a failure
immediately stops the instance search. In contrast, \elpi modes do not check the
shape of the argument before backchain the query into the database, it, instead,
modifies the unification strategy by using the \matching procedure.

In order to see the different mode behavior of the \rocq and \elpi modes, we
rework the implementation of the \cI{Add} type class.
%
\input{code/add_dummy.tex}

Consider a goal $g = \cI{Add ?X}$ where \cI{?X} is a meta-variable (evar in
\rocq jargon). With the \rocq mode system, this goal has no solution because the
argument of the class is a non-ground term and therefore does not satisfy the
mode constraint. In contrast, under the \elpi mode interpretation the instance
\cI{addDummy} is a valid solution.

In the \elpi philosophy, the instance \cI{addDummy} is considered as a catchall:
the first argument of the \cI{Add} class is a variable, i.e a pattern that can
be used a on any goal for \cI{Add}. It means that \cI{addDummy} can be used as a
solution for $g$.



The second discrepancy we point out conserning modes in \rocq, is that a type
class can be associated to several modes. This is the case of the class
\eI{Cancel} Note: TCDiag in stdpp is an example of a class where the two modes is needed,
elpi can't implement it

Guardare: Typeclasses Strict Resolution

Modes in \rocq are very dynamic, they can be changed at any point in the code
by the code and in elpi this is not possible in elpi since \dots

TODO: dire che i modi dichiarati a posteriori cambiano il comportamento della
search, potremmo avere due file con diversi in cui in uno si setta il modo e
nell'altro no, allora si hanno due comportamenti diversi.


\section{Instance compilation}
\label{sec:instance-encoding}

\begin{figure}
  \input{code/toy_compiler.tex}
  \caption{Instance compiler}
  \label{fig:simple-elpi-compiler}
\end{figure}

In \rocq, instance compilation is triggered immediately after an instance
declaration. \Cref{fig:simple-elpi-compiler} shows an instance compiler
implemented in \elpi where we take more care about the type of dependent
quantification and the polarity of the instance being compiled is an essential
ingridient for compiling deep hereditary Harrop formulas.

Similarly to \cref{fig:simple-elpi-compiler-tc}, we define a \eI{compile}
predicate which calls the \eI{comp} auxiliary function. \eI{comp} this time
takes one more argument which is a boolean and tells the polarity of an element.

The compilation proceeds by analyzing the structure of the instance type.
There are three relevant cases. Rules \eI{~\PYG{c}{r1}~} and
\eI{~\PYG{c}{r2}~} handle quantifications, while rule
\eI{~\PYG{c}{r3}~} is the base case that constructs the final \elpi rule
from the information accumulated during the recursive traversal.

When the current type has the form $\forall \eI{x}: \eI{Ty}, \eI{Bo x}$, the
abstraction binds a variable of type \eI{Ty} in the body \eI{Bo}. The test
\eI{is-class? Ty} checks whether \eI{Ty} has the shape $\forall x_1 \dots x_n,
T$ (possibly with zero quantifiers), where \eI{T} is a term whose applicative
head is a type class.

If this condition holds, we are compiling a conditional instance and rule
\eI{~\PYG{c}{r$\to$}~} is applied. The rule introduces a fresh nominal variable
\eI{x} representing both the abstracted variable in \eI{Bo} and the local
instance with type \eI{Ty}. To construct the premise
corresponding to \eI{x}, the compiler recursively invokes \eI{comp} on \eI{x}
while switching the polarity of the rule. This recursive call produces the
premise \eI{M}, which abstracts \eI{x}. Compilation then continues on
\eI{Bo x}, while extending the accumulators with the new instance argument
\eI{x} and the new premise \eI{M x}.

Rule \eI{~\PYG{c}{r$\forall$}~} handles the case where the quantified type is
not a type class. In this situation, the compiler behaves exactly as rule
\eI{~\PYG{c}{r$\forall$}~} in \cref{fig:simple-elpi-compiler-tc}: it simply
consumes under the abstraction, leaving the list of premises \eI{P} unchanged
and adding the variable \eI{x} to the list of instance arguments.

As in \cref{fig:simple-elpi-compiler-tc}, the base case of \eI{comp} is reached
when the current type \eI{Ty} is not a quantification. In this case, \eI{Ty} is
a type class \eI{C} applied to a list of arguments \eI{CAg}. The compiler then
constructs the final rule by building its head and premises.

To build the head \eI{Head}, the compiler first constructs the proof term
\eI{Proof}, obtained by applying the instance \eI{I} to its arguments
\eI{rev Ag}. The list \eI{Ag} is stored in reverse order during traversal, so
it is reversed before constructing the application. The predicate
\eI{make-rule-head} takes the class name as its first argument in order to
generate the predicate corresponding to the class. The arguments of this
predicate are obtained by appending the proof term to the list of class
arguments.

The premises of the clause are stored in the accumulator \eI{P}. As with the
instance arguments, they are reversed before constructing the final rule.

Finally, the predicate \eI{build-rule} assembles the rule according to the
current polarity. With positive polarity, it produces a standard clause
\eI{Head :- Prems}. With negative polarity, it produces the implication
\eI{Prems => Head}.

\begin{figure}
  \input{code/HORN1.tex}
  \caption{Example with horn logic}
  \label{fig:h-clause}
\end{figure}

Given the instance in \cref{fig:add-class-full}, this version of the
compiler produces the following rules:

\begin{elpicode}
  tc-Add {{nat}} {{addNat}}.
  tc-Add {{R}} {{addR}}.
  pi L \ pi R \ pi PL \ pi PR \
    tc-Add {{lp:L * lp:R}} {{addProd lp:L lp:R lp:PL lp:PR}} :- 
      tc-Add L PL, tc-Add R PR.
\end{elpicode}

\noindent
These rules are similar to those produced by the compiler in
\cref{sec:simple-instance-compiler}, except for the specialization of the
predicate \eI{tc-Add} for the \cI{Add} class.

An interesting example of deep compilation, where polarities play a
central role (and are incorrectly handled by the compiler in
\cref{sec:simple-instance-compiler}), is shown in
\cref{fig:h-clause}. The code illustrates an encoding of a fragment of
propositional logic using the \rocq{} typeclass mechanism as a proof
search engine.

We define an inductive type \texttt{horn} representing a restricted
language of formulas consisting of atomic facts, conjunctions, and
implications. The constructor \texttt{fact} embeds a \rocq{}
proposition into the object language, while \texttt{and} and
\texttt{impl} correspond to conjunction and implication,
respectively. In the case of \texttt{impl}, the antecedent is
restricted to a \rocq{} proposition rather than a \texttt{horn}
formula.

The function \cI{interp} assigns a semantics to \texttt{horn}
formulas by translating them into \rocq{} propositions. This
interpretation is compositional: a fact evaluates to its underlying
proposition, a conjunction is interpreted as a logical conjunction,
and an implication as a function type.

Automatic reasoning is expressed via the typeclass
\texttt{Provable T}, which packages a proof of \texttt{interp T}.
This allows \rocq{}'s typeclass resolution mechanism to act as a
proof search procedure.

The inference rules of the logic are encoded as typeclass
instances. The instance \texttt{Pand} corresponds to conjunction
introduction: if \texttt{F1} and \texttt{F2} are provable, then
\texttt{and F1 F2} is also provable.

Similarly, the instance \texttt{Pimpl} captures implication
introduction. It states that to prove \texttt{impl F1 F2}, it
suffices to show that \texttt{F2} is provable under the assumption
\texttt{F1}, represented as \texttt{fact F1}.

The instance \cI{Pand} is compiled into:

\begin{elpicode}
  pi L \ pi R \ pi PL \ pi PR \
    tc-Provable {{and lp:L lp:R}} {{Pand lp:L lp:R lp:PL lp:PR}} :- 
      tc-Provable L PL, tc-Provable R PR.
\end{elpicode}

\noindent
This rule closely mirrors the rule for \cI{addProd}, since both rely on the same
search principle: constructing a proof requires recursively constructing proofs
for the subcomponents (the arguments of \cI{and}, respectively \cI{*}).

The rule corresponding to the instance \cI{Pimpl} is more subtle,
since it involves a higher-order hypothesis. Its type is:

$$
\forall \cI{F1 F2},
\underbrace{
  (\underbrace{\cI{Provable (fact F1)}}_{positive} \to
   \underbrace{\cI{Provable F2}}_{negative})
}_{negative}
\to
\underbrace{\cI{Provable (impl F1 F2)}}_{positive}
$$

The compiled rule is:

\begin{elpicode}
  pi F B Q \
    tc-Provable {{impl lp:F lp:B}} {{Pimpl lp:F lp:B lp:Q}} :- 
      pi p \ tc-Provable {{fact lp:F}} p => tc-Provable B {{lp:Q lp:p}}.
\end{elpicode}

The structure of this rule matches the intended semantics: the head
targets \cI{impl F B}, while the body attempts to construct a proof
of \cI{B} in a context extended with the assumption \cI{F}.

However, despite appearing correct, this rule is insufficient to
prove queries such as
$q = \eI{tc-Provable {{impl a (fact a)}} R}$, for some proposition
\eI{a}. After applying the rule for \cI{Pimpl}, we must solve the
sub-query
$q' = \eI{tc-Provable {{fact a}} {{lp:Q lp:p}}}$ in a context
extended with the hypothesis
$h = \eI{tc-Provable {{fact a}} p}$.

Intuitively, the hypothesis $h$ should solve $q'$ by instantiating
$Q$ with the identity function. However, this unification fails in
\elpi{}. The reason is that the higher-order abstract syntax (HOAS)
term corresponding to \eI{{{lp:Q lp:p}}} is \eI{app[Q, p]}, which
cannot be unified with \eI{p}, whose HOAS representation is simply
\eI{p}.

This failure arises from a mismatch between \elpi application and
\rocq application. In \rocq, unification would succeed by instantiating
\eI{Q} with \cI{fun x => PAtom F x}. However, as discussed in
\cref{sec:unif-fo-ho}, \elpi does not perform this higher-order
unification on \rocq terms.

The rule should be compiled into\marginpar{TODO}

\begin{elpicode}
  pi F1 F2 Q \
    tc-Provable {{impl lp:F1 lp:F2}} {{Pimpl lp:F1 lp:F2 lp:Q}} :- 
      link Q Q',
      pi p \ tc-Provablefact F1 p => tc-Provable F2 (Q' p).
\end{elpicode}

\noindent
where \eI{Q'} is a unification variable linked to \dots To address this issue,\marginpar{TODO}
we dedicate the full \cref{chap:ho-for-free}


\subsection{Rule Priorities}

When declaring instances, the priority assigned to each rule plays a
fundamental role in the performance of the resolution phase. In \rocq,
every instance is associated with a priority at declaration time.

To support rule priorities, we extend the instance trigger so that it
also propagates priority information to the \elpi compiler. To reproduce
a similar behavior in \elpi, we exploit its grafting mechanism, which
allows rules in the database to be named and enables new rules to be
inserted before or after existing ones.

In the \elpi compiler, we predeclare a fixed number of hooks, each
associated with an integer $n$ representing a grafting position. We
account for this mechanism by modifying the \eI{compile} rule in
\cref{fig:simple-elpi-compiler} so that it takes this integer as input.
When a rule is added to the database, we explicitly specify that it must
be grafted at position $n$.\marginpar{before, not at}

\subsection{Goal Encoding}
\label{sec:goal-encoding}

\begin{figure}
  \input{code/toy_compiler_solver.tex}
  \caption{Goal compiler}
  \label{fig:goal-compiler}
\end{figure}

With the database compilation in place, we now define the tactic used to
solve type-class goals. The implementation of the \eI{solve} predicate is
shown in \cref{fig:goal-compiler}.

A \rocq goal in \elpi contains, among other information, the context
\eI{C} in which the goal is defined and a term \eI{Ty} representing the
type to be solved. The first step is to traverse the context, select the
hypotheses that correspond to type classes, and compile them using the
\eI{comp} procedure. This produces a list of local rules \eI{H}, which
are added to the program during resolution.

Next, we compile the goal type \eI{Ty}. As in hereditary Harrop formulas,
a goal corresponds to an atom in negative position. Therefore, the
compilation is performed with polarity \eI{ff}. Concretely,
%
\begin{center}
  \eI{comp ff P Ty [] H R}
\end{center}

\noindent
constructs an atom in negative position. Here, \eI{P} is a unification
variable representing the proof term, \eI{Ty} is the goal to be compiled,
the list of instance parameters is empty, and \eI{H} provides the
premises extracted from the context. The result of the compilation is an
atom \eI{R} of the form \eI{H => Q}, where \eI{Q} is a query corresponding
to a type class whose proof argument is \eI{P}.

Evaluating \eI{R} instantiates the proof term \eI{P}, which is then used
to refine the original \rocq goal.

\subsection{User-Defined Rules}

In addition to the automatic compilation of rules, users can extend the
database with custom rules to implement specialized or more efficient
resolution strategies in specific situations.

Using the \cI{Elpi Accumulate} command, users can define ad hoc rules,
such as the one shown below:

\input{code/share_proof.tex}

This rule matches products whose two components are identical. When
searching for an instance of the class \cI{Add} applied to a type \eI{A},
both components of the pair yield the same result, so the search is
performed only once. Moreover, the resulting proof term shares memory
between subproofs and their types, improving space and time efficiency.

In the simple rule presented above, we illustrate the expressive power of
meta-programming in \elpi. Since \elpi is a Turing-complete language, users
can arbitrarily customize the proof search by defining their own rules.
In particular, there are no restrictions on these extensions:
users are free to implement any custom rule that suits their needs. 

\section{Experimental results}

We have tested our solver on existing libraries, in particular, we have focused
our attention on two main libraries \tlc~\cite{tlc} and \stdpp~\cite{stdpp},
they are two libraries exploiting the type-class inference mechanism as a
principal feature of their implementation. Studying these libraries allows to
have concrete example about what are the main need of a \rocq developers using
type classes. Apart from automation, and in particular in \stdpp, modes play an
important role in the resolution of type-class goals, they allows to identify
potential bugs in our implementation and some research directions.
In this experemantiation, an undoubtely interesting aspect is benchmarking our
implementation against the \rocq sover. 

% TODO: spiegare meglio l'iniettività, come è implementata
We perform a first bench from some classes and instances taken from the \stdpp
library. The test deals with the \cI{Inj} typeclass defined as follows:

\begin{coqcode}
  Class Inj {A B} (R : relation A) (S : relation B) (f : A → B) : Prop :=
    inj x y : S (f x) (f y) → R x y.
\end{coqcode}

It claims that a function \cI{f} is injective wrt two relations \cI{R} and
\cI{S}, if \dots

The instances implementing this typeclass are $13$ and treat, for example, the
injectivity of the identity (\cI{id}), the \cI{inl} and \cI{inr}\footnote{which
return the left and right projection of the sum sype} with \cI{R} and \cI{S}
fixed to the \cI{=} relation. The interesting instance in our benchmark is the
injectivity of function composition, which tells that the composition (noted
$\circ$) of two functions \cI{f} and \cI{g} is injective if the two functions
are both injective.

In our benchmark we profile the time taken by the instance solver in the
resolution of goals growing in size to check where the resolution time is spent
during the search and compare the performance of the \elpi solver against \rocq
solver. The type-class goal is built through the auxiliary function, defined
below, which build a perfectly balanced tree with the compose function as the
inner nodes and the function $f$ (assumed injective) as leaves.

\[
g_n =
\begin{cases}
f & \text{if } n = 0 \\
g_{n-1} \circ g_{n-1} & \text{if } n > 0
\end{cases}
\]

\begin{figure}
  \begin{subfigure}{0.45\textwidth}
    \includegraphics[width=1\textwidth]{img/bench/elpi_stat_check.pdf}
    \caption{Bench with \eI{refine}}
    \label{fig:bench-1}
  \end{subfigure}
  \begin{subfigure}{0.45\textwidth}
    \includegraphics[width=1\textwidth]{img/bench/elpi_stat_no_check.pdf}
    \caption{Bench with \eI{refine.no_check}}
    \label{fig:bench-2}
  \end{subfigure}
\end{figure}

We submit the goal \cI{Inj eq eq ~$g_n$~} for a given $n$ to the solver: the
goal has an exponential size wrt the parameter $n$. In \cref{fig:bench-1} we
draw the result of the resolution of such a goal for $n$ going from $0$ to $15$.
The $x$ axis counts for the number of nodes in the tree and uses a logarithm
scale. The $y$ axis represents the time in seconds.
The \elpi time is decomposed in $4$ main parts,

Ci sono due bench possibili: in ogni caso dobbiamo passare per il backchaining
che trova una soluzione, dopo di che dobbiamo ritornanre la soluzione a rocq. La
classica strategia consiste nel fare un refine della soluzione con il goal in
modo da identificare i sottogoal da spedire a coq. Ora, in elpi abbiamo diversi
refine che perfmettono di effettuare questa operazione, ma che differiscono a
secondo in cui il goal viene anche typecheckato o no. In linea generale la prova
che restituisce il solver non include constraint sui vincoli di universo.
Quindi, in un goal dove non servono unisversi, possiamo immaginare che la
soluzione restituita dal checker ha il tipo giusto per il goal ricevuto, si può
quindi usare il \cI{refine.no_check}. Viceversa, bisogna che i vincoli di
universo siano imposti e quindi bisogna passare per \cI{refine.typecheck}.

Facciamo un primo bench con elpi in cui si frazionano i tempi di ricerca. I
tempi includono il refine, la ricerca, qulche overhead. Notiamo che il refine è
la bestia che ci consuma gran parte del tempo totale. Quindi, dal nostro punto
di vista, non ha senso ottimizzare ulteriormente il tempo di ricerca, dato che
non influisce sul tempo di ricerca totale in modo significativo. COnfrontiamo
il tempo con rocq è ci rendiamo conto che alla fine, anche usando il solver
più lento (con vincoli di universo) siamo comunque competitivi.

Una soluzione sarebbe quella di comilare le regole usando i vincoli di universo
come nuove premesse delle regole compilate, ma questo è ingegneria del codice
che prende tempo e quindi ci affacciamo su un altro problema che \dots

In realtà il nostro solver non uccide quello di rocq, abbiamo la possibilità di
usare uno o l'altro a seconda di un'opzione che attiva o disattiva x o y.



After these experemantiation, an undoubtely key factor of the type class solver
è l'unificazione. 

Dire che nella compilazione un problema di unificazione è dato dagli universi
che noi buchiamo, lasciamo all'unificazione il compito di risolverli.

TODO: disattivare il ho-compiler e vedere quanti se ne rompono in più. Devono
essere regole che parlano di unificazione superiore. Un esempio è quello della
sezione sopra.

In irisi i problemi diventano molti di più e sono dovuti a coercion e strutture
canoniche che nell'unificazione non gestimo. Una soluzione sarebbe riconoscere
quando una possibile coercion viene introdotta e 

Il problema in stdpp dei modi è dovuto all'implementazione delle clasi come
ElemOf che vuole in input due type slegati. L'essere slegato impedisce di
deudrre un tipo dall'altro e quindi, se il modo è in unt di elpi, allora ci
ritroviamo in una situazione in cui il goal è

ElemOf Type -> Type -> Type.

ElemOf A (option ?X), con l'ipotesi Elem A (option A), che in elpi fallisce. Il
modo per risolvere questo issue consiste nel cambiare il tipo della classe che
diventerebbe

ElemOF (A : Type) (M : Type -> Type) -> Type e il campo diventa:
elem\_of A -> (M A) -> Prop.

Con i modi di rocq funziona, me, nel caso in cui ci fossreo due regole ElmOf ?A
(option bool) \_, ElemOf ?A (option string) quella con priorità maggiore verrebbe
usata, istanziando ?A. Cosa che va contro la politica input/output di elpi.

Dire di alcune opzioni che si possono usare in rocq. (Forse spostare in
contesto).





\subsectionTwo{\texorpdfstring{$\eta\beta$}{eta/beta}-unification}
\subsectionTwo{Term unfolding: \texorpdfstring{$\delta$}{delta}-unification}

BLI BLI BLA BLAs

Chiudere con: our aim is not to compete or replace the rocq solver, we are
proposing an alternative which may help the user to perform instance search in a
highly customizable runtime. Some limitations in our develpment remains, and
are double face: in \dots

Dire che scrivere il solver in \elpi da beneficio indiretto delle features del
linguaggio che sono ereditate e gratuite. Una su tutte il backtracking, ma anche
i modi. Infine abbiamo la determinacy. Un esempio è questo: ... dimostra come
... ma non l'abbiamo mai testata in una vera libreria...

TODO: Quali sono i comandi che settano, unsettano la search?

TODO: ci sono dei comandi che iterano il database di classi e istanze per
aggiungerle tutte in un botto

Una roba di cui non si è mai parlato è l'importanza del cut. In questo momento
non abbiamo alcuna sintassi rocq che permette di scegliere dove e come metter il
cut in una regola che deve essere compilata. Cambiare il compilatore e prendere
questa feature in considerazione per avvicinare ulteriormente la risoluzione di
classi a un sistema di programmazione logica con cut ... 

\section{Discussion}
\label{sec:tc-discussion}

From the previous sections, we can see how flexible the \elpi language is and
how well-fits for \rocq term compilation into \elpi rules. \elpi can be used as
a way to introduce some kind of proof automation: the \prolog-like search engine
of its interpreter acts as a good inference system provided that the program in
which a query is executed is inhabited with the (derivation) rules that are
needed to solve the query. The type-class engine written in the \rocq system has
this \prolog-like behavior, but it is important to note that the goal of our
type-class solver is not to faithfully reproduce the behavior of the \rocq
solver. Instead, it aims to exploit the features of the \elpi language whenever
possible. 

As anticipated in \cref{??}, \elpi allows the user to control the unification
strategy, \unify vs \matching depending of the mode. The cut operator allow the
user to commit choice points avoiding unwanted backtracking.



\subsection{Rule indexing: discrimination trees}
\label{sec:rule-indexing}


\newcommand{\pfdt}{https://www.cs.cmu.edu/~fp/courses/99-atp/lectures/lecture28.html}

Discrimination trees: Pfenning notes~\footnote{\url{\pfdt}}

TODO: do some benchmarks

Discrimination trees: opt for lists and tree depths.


\cite{tabling-dm,tabling-pie}

